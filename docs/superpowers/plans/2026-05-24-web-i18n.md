# Web I18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add scalable `en` and `zh-CN` multilingual support to the existing web control plane using `i18next` and `react-i18next`, including persisted locale selection, authenticated and unauthenticated language switching, translated frontend-owned UI copy, and locale-aware certificate date formatting.

**Architecture:** Keep the existing Vite + React + hash-router app shape. Introduce a dedicated `src/lib/i18n` runtime with locale normalization, resource namespaces, and `localStorage` persistence. Translate UI at the render layer only, leaving API payloads and stored enum values unchanged. Add a small shared `LanguageSwitcher` component and migrate page copy incrementally behind tests.

**Tech Stack:** React 18, TypeScript, Vite, Vitest, i18next, react-i18next, Testing Library

---

## File Map

### Create

- `packages/web/src/lib/i18n/config.ts`
- `packages/web/src/lib/i18n/index.ts`
- `packages/web/src/lib/i18n/resources/index.ts`
- `packages/web/src/lib/i18n/resources/common.ts`
- `packages/web/src/lib/i18n/resources/layout.ts`
- `packages/web/src/lib/i18n/resources/login.ts`
- `packages/web/src/lib/i18n/resources/accessLogin.ts`
- `packages/web/src/lib/i18n/resources/routes.ts`
- `packages/web/src/lib/i18n/resources/authRules.ts`
- `packages/web/src/lib/i18n/resources/users.ts`
- `packages/web/src/lib/i18n/resources/certificates.ts`
- `packages/web/src/lib/i18n/resources/settings.ts`
- `packages/web/src/test/render.tsx`
- `packages/web/src/lib/i18n/index.test.ts`
- `packages/web/src/components/LanguageSwitcher.tsx`
- `packages/web/src/pages/LoginPage.test.tsx`
- `packages/web/src/components/Layout.test.tsx`
- `packages/web/src/components/DataTable.test.tsx`
- `packages/web/src/pages/CertificatesPage.test.tsx`

### Modify

- `packages/web/package.json`
- `packages/web/package-lock.json`
- `packages/web/src/test/setup.ts`
- `packages/web/src/main.tsx`
- `packages/web/src/components/Layout.tsx`
- `packages/web/src/components/PageHeader.tsx`
- `packages/web/src/components/DataTable.tsx`
- `packages/web/src/components/RouteForm.tsx`
- `packages/web/src/components/UserForm.tsx`
- `packages/web/src/components/AuthRuleForm.tsx`
- `packages/web/src/components/CertificateForm.tsx`
- `packages/web/src/components/ui/Table.tsx`
- `packages/web/src/components/ui/Modal.tsx`
- `packages/web/src/pages/LoginPage.tsx`
- `packages/web/src/pages/AccessLoginPage.tsx`
- `packages/web/src/pages/RoutesPage.tsx`
- `packages/web/src/pages/AuthRulesPage.tsx`
- `packages/web/src/pages/UsersPage.tsx`
- `packages/web/src/pages/CertificatesPage.tsx`
- `packages/web/src/pages/SettingsPage.tsx`

### Tests

- `packages/web/src/lib/i18n/index.test.ts`
- `packages/web/src/pages/LoginPage.test.tsx`
- `packages/web/src/components/Layout.test.tsx`
- `packages/web/src/components/DataTable.test.tsx`
- `packages/web/src/pages/CertificatesPage.test.tsx`

## Task 1: Add I18n Runtime, Locale Detection, and Test Harness

**Files:**
- Create: `packages/web/src/lib/i18n/config.ts`
- Create: `packages/web/src/lib/i18n/index.ts`
- Create: `packages/web/src/lib/i18n/resources/index.ts`
- Create: `packages/web/src/lib/i18n/resources/common.ts`
- Create: `packages/web/src/lib/i18n/resources/layout.ts`
- Create: `packages/web/src/lib/i18n/resources/login.ts`
- Create: `packages/web/src/lib/i18n/resources/accessLogin.ts`
- Create: `packages/web/src/test/render.tsx`
- Create: `packages/web/src/lib/i18n/index.test.ts`
- Modify: `packages/web/package.json`
- Modify: `packages/web/package-lock.json`
- Modify: `packages/web/src/test/setup.ts`
- Test: `packages/web/src/lib/i18n/index.test.ts`

- [ ] **Step 1: Add runtime and test dependencies**

Run:

```bash
npm --prefix packages/web install i18next react-i18next
npm --prefix packages/web install --save-dev @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

Expected:

```text
packages/web/package.json and packages/web/package-lock.json now include i18next, react-i18next, and Testing Library packages.
```

- [ ] **Step 2: Write the failing locale selection and persistence tests**

Create `packages/web/src/lib/i18n/index.test.ts`:

```tsx
import { beforeEach, describe, expect, it, vi } from 'vitest'

describe('app i18n runtime', () => {
  beforeEach(() => {
    localStorage.clear()
    vi.resetModules()
  })

  it('prefers the persisted locale over browser language', async () => {
    localStorage.setItem('auth-gate.locale', 'zh-CN')
    vi.stubGlobal('navigator', {
      language: 'en-US',
      languages: ['en-US', 'en'],
    })

    const { createAppI18n } = await import('./index')

    const i18n = await createAppI18n()

    expect(i18n.resolvedLanguage).toBe('zh-CN')
  })

  it('maps browser zh variants to zh-CN when nothing is persisted', async () => {
    vi.stubGlobal('navigator', {
      language: 'zh-TW',
      languages: ['zh-TW', 'en-US'],
    })

    const { createAppI18n } = await import('./index')

    const i18n = await createAppI18n()

    expect(i18n.resolvedLanguage).toBe('zh-CN')
  })

  it('falls back to en when persisted and browser locales are unsupported', async () => {
    localStorage.setItem('auth-gate.locale', 'fr-FR')
    vi.stubGlobal('navigator', {
      language: 'fr-FR',
      languages: ['fr-FR'],
    })

    const { createAppI18n } = await import('./index')

    const i18n = await createAppI18n()

    expect(i18n.resolvedLanguage).toBe('en')
  })

  it('persists manual language changes', async () => {
    vi.stubGlobal('navigator', {
      language: 'en-US',
      languages: ['en-US'],
    })

    const { createAppI18n } = await import('./index')

    const i18n = await createAppI18n()
    await i18n.changeLanguage('zh-CN')

    expect(localStorage.getItem('auth-gate.locale')).toBe('zh-CN')
  })
})
```

- [ ] **Step 3: Run the test to verify it fails**

Run:

```bash
npm --prefix packages/web test -- src/lib/i18n/index.test.ts
```

Expected:

```text
FAIL because ./index does not exist yet.
```

- [ ] **Step 4: Add the i18n config, resources, and reusable test renderer**

Create `packages/web/src/lib/i18n/config.ts`:

```ts
export const SUPPORTED_LOCALES = ['en', 'zh-CN'] as const

export type AppLocale = (typeof SUPPORTED_LOCALES)[number]

export const DEFAULT_LOCALE: AppLocale = 'en'
export const LOCALE_STORAGE_KEY = 'auth-gate.locale'

export function normalizeLocale(raw: string | null | undefined): AppLocale | null {
  if (!raw) return null

  const lower = raw.toLowerCase()

  if (lower === 'en' || lower.startsWith('en-')) {
    return 'en'
  }

  if (lower === 'zh' || lower === 'zh-cn' || lower.startsWith('zh-')) {
    return 'zh-CN'
  }

  return null
}

export function detectInitialLocale() {
  const persisted = normalizeLocale(localStorage.getItem(LOCALE_STORAGE_KEY))
  if (persisted) return persisted

  const languages = Array.isArray(navigator.languages) ? navigator.languages : []
  for (const candidate of languages) {
    const normalized = normalizeLocale(candidate)
    if (normalized) return normalized
  }

  return normalizeLocale(navigator.language) ?? DEFAULT_LOCALE
}
```

Create `packages/web/src/lib/i18n/resources/common.ts`:

```ts
export const commonEn = {
  actions: {
    cancel: 'Cancel',
    closeModal: 'Close modal',
    delete: 'Delete',
    edit: 'Edit',
  },
  table: {
    actions: 'Actions',
    noData: 'No data',
  },
} as const

export const commonZhCN = {
  actions: {
    cancel: '取消',
    closeModal: '关闭弹窗',
    delete: '删除',
    edit: '编辑',
  },
  table: {
    actions: '操作',
    noData: '暂无数据',
  },
} as const
```

Create `packages/web/src/lib/i18n/resources/layout.ts`:

```ts
export const layoutEn = {
  brand: {
    name: 'Auth Gate',
    controlPlane: 'Control Plane',
    currentFocus: 'Current Focus',
  },
  navigation: {
    main: 'Main navigation',
    mobile: 'Mobile navigation',
    openMenu: 'Open menu',
    closeMenu: 'Close menu',
  },
  user: {
    logout: 'Logout',
  },
  sections: {
    routes: {
      label: 'Routes',
      description: 'Traffic orchestration',
    },
    certificates: {
      label: 'Certificates',
      description: 'TLS automation',
    },
    auth: {
      label: 'Auth Rules',
      description: 'Policy enforcement',
    },
    users: {
      label: 'Users',
      description: 'Directory and access',
    },
    settings: {
      label: 'Settings',
      description: 'Runtime operations',
    },
  },
} as const

export const layoutZhCN = {
  brand: {
    name: 'Auth Gate',
    controlPlane: '控制台',
    currentFocus: '当前焦点',
  },
  navigation: {
    main: '主导航',
    mobile: '移动端导航',
    openMenu: '打开菜单',
    closeMenu: '关闭菜单',
  },
  user: {
    logout: '退出登录',
  },
  sections: {
    routes: {
      label: '路由',
      description: '流量编排',
    },
    certificates: {
      label: '证书',
      description: 'TLS 自动化',
    },
    auth: {
      label: '鉴权规则',
      description: '策略执行',
    },
    users: {
      label: '用户',
      description: '目录与访问',
    },
    settings: {
      label: '设置',
      description: '运行时操作',
    },
  },
} as const
```

Create `packages/web/src/lib/i18n/resources/login.ts`:

```ts
export const loginEn = {
  hero: {
    eyebrow: 'Secure Routing Workspace',
    title: 'Manage gateway traffic with a calmer, sharper control surface.',
    description: 'Configure routes, apply authentication policies, and perform runtime operations from one focused console.',
    routingLabel: 'Routing',
    routingValue: 'Precise matching',
    authLabel: 'Auth',
    authValue: 'Policy per route',
    runtimeLabel: 'Runtime',
    runtimeValue: 'Safe reload operations',
  },
  card: {
    eyebrow: 'Auth Gate',
    title: 'Sign in to the control plane',
    description: 'Use an administrator account to manage routes, authentication rules, and runtime operations.',
    submit: 'Sign In',
    sessionHint: 'Session access is scoped by your server-side permissions.',
  },
  fields: {
    username: 'Username',
    usernamePlaceholder: 'Enter username',
    password: 'Password',
    passwordPlaceholder: 'Enter password',
  },
  errors: {
    network: 'Network error',
  },
} as const

export const loginZhCN = {
  hero: {
    eyebrow: '安全路由工作台',
    title: '用更冷静、更清晰的控制界面管理网关流量。',
    description: '在一个专注的控制台中配置路由、应用鉴权策略并执行运行时操作。',
    routingLabel: '路由',
    routingValue: '精准匹配',
    authLabel: '鉴权',
    authValue: '按路由设定策略',
    runtimeLabel: '运行时',
    runtimeValue: '安全热重载',
  },
  card: {
    eyebrow: 'Auth Gate',
    title: '登录控制台',
    description: '使用管理员账号管理路由、鉴权规则和运行时操作。',
    submit: '登录',
    sessionHint: '会话访问范围由服务端权限控制。',
  },
  fields: {
    username: '用户名',
    usernamePlaceholder: '输入用户名',
    password: '密码',
    passwordPlaceholder: '输入密码',
  },
  errors: {
    network: '网络错误',
  },
} as const
```

Create `packages/web/src/lib/i18n/resources/accessLogin.ts`:

```ts
export const accessLoginEn = {
  eyebrow: 'Route Access',
  title: 'Sign in to continue',
  description: 'This route is protected by Auth Gate. Sign in with a user account that has access to {{routeName}}.',
  submit: 'Continue to Route',
  fields: {
    username: 'Username',
    usernamePlaceholder: 'Enter username',
    password: 'Password',
    passwordPlaceholder: 'Enter password',
  },
  errors: {
    network: 'Network error',
  },
} as const

export const accessLoginZhCN = {
  eyebrow: '路由访问',
  title: '登录后继续',
  description: '该路由受 Auth Gate 保护。请使用有权访问 {{routeName}} 的账号登录。',
  submit: '继续访问路由',
  fields: {
    username: '用户名',
    usernamePlaceholder: '输入用户名',
    password: '密码',
    passwordPlaceholder: '输入密码',
  },
  errors: {
    network: '网络错误',
  },
} as const
```

Create `packages/web/src/lib/i18n/resources/index.ts`:

```ts
import { accessLoginEn, accessLoginZhCN } from './accessLogin'
import { commonEn, commonZhCN } from './common'
import { layoutEn, layoutZhCN } from './layout'
import { loginEn, loginZhCN } from './login'

export const resources = {
  en: {
    common: commonEn,
    layout: layoutEn,
    login: loginEn,
    accessLogin: accessLoginEn,
  },
  'zh-CN': {
    common: commonZhCN,
    layout: layoutZhCN,
    login: loginZhCN,
    accessLogin: accessLoginZhCN,
  },
} as const
```

Create `packages/web/src/lib/i18n/index.ts`:

```ts
import i18next, { type i18n as I18nInstance } from 'i18next'
import { initReactI18next } from 'react-i18next'
import {
  DEFAULT_LOCALE,
  LOCALE_STORAGE_KEY,
  detectInitialLocale,
  normalizeLocale,
  type AppLocale,
} from './config'
import { resources } from './resources'

export async function createAppI18n(initialLocale?: AppLocale): Promise<I18nInstance> {
  const i18n = i18next.createInstance()

  await i18n.use(initReactI18next).init({
    resources,
    lng: initialLocale ?? detectInitialLocale(),
    fallbackLng: DEFAULT_LOCALE,
    supportedLngs: ['en', 'zh-CN'],
    defaultNS: 'common',
    interpolation: {
      escapeValue: false,
    },
  })

  i18n.on('languageChanged', (nextLanguage) => {
    const normalized = normalizeLocale(nextLanguage) ?? DEFAULT_LOCALE
    localStorage.setItem(LOCALE_STORAGE_KEY, normalized)
  })

  return i18n
}

export const i18nPromise = createAppI18n()
```

Modify `packages/web/src/test/setup.ts`:

```ts
import '@testing-library/jest-dom/vitest'
import { afterEach, vi } from 'vitest'

const storage = new Map<string, string>()

Object.defineProperty(globalThis, 'localStorage', {
  value: {
    getItem: (key: string) => storage.get(key) ?? null,
    setItem: (key: string, value: string) => {
      storage.set(key, value)
    },
    removeItem: (key: string) => {
      storage.delete(key)
    },
    clear: () => {
      storage.clear()
    },
  },
  configurable: true,
})

afterEach(() => {
  vi.restoreAllMocks()
  localStorage.clear()
})
```

Create `packages/web/src/test/render.tsx`:

```tsx
import { render, type RenderOptions } from '@testing-library/react'
import { I18nextProvider } from 'react-i18next'
import type { ReactElement } from 'react'
import { createAppI18n } from '../lib/i18n'
import type { AppLocale } from '../lib/i18n/config'

export async function renderWithI18n(
  ui: ReactElement,
  options?: RenderOptions & { locale?: AppLocale }
) {
  const i18n = await createAppI18n(options?.locale)
  const renderResult = render(<I18nextProvider i18n={i18n}>{ui}</I18nextProvider>, options)
  return Object.assign(renderResult, { i18n })
}
```

- [ ] **Step 5: Run the test to verify it passes**

Run:

```bash
npm --prefix packages/web test -- src/lib/i18n/index.test.ts
```

Expected:

```text
PASS src/lib/i18n/index.test.ts
```

- [ ] **Step 6: Commit**

```bash
git add packages/web/package.json packages/web/package-lock.json packages/web/src/test/setup.ts packages/web/src/test/render.tsx packages/web/src/lib/i18n
git commit -m "feat(web): add i18n runtime and test harness"
```

## Task 2: Wire the App Entry, Add the Language Switcher, and Translate Unauthenticated Screens

**Files:**
- Create: `packages/web/src/components/LanguageSwitcher.tsx`
- Create: `packages/web/src/pages/LoginPage.test.tsx`
- Create: `packages/web/src/components/Layout.test.tsx`
- Modify: `packages/web/src/main.tsx`
- Modify: `packages/web/src/components/Layout.tsx`
- Modify: `packages/web/src/pages/LoginPage.tsx`
- Modify: `packages/web/src/pages/AccessLoginPage.tsx`
- Modify: `packages/web/src/lib/i18n/resources/layout.ts`
- Modify: `packages/web/src/lib/i18n/resources/login.ts`
- Modify: `packages/web/src/lib/i18n/resources/accessLogin.ts`
- Test: `packages/web/src/pages/LoginPage.test.tsx`
- Test: `packages/web/src/components/Layout.test.tsx`

- [ ] **Step 1: Write the failing tests for untranslated login copy and missing language switcher**

Create `packages/web/src/pages/LoginPage.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import { describe, expect, it, vi } from 'vitest'
import { LoginPage } from './LoginPage'
import { renderWithI18n } from '../test/render'

describe('LoginPage i18n', () => {
  it('renders translated login copy in zh-CN', async () => {
    await renderWithI18n(<LoginPage onLogin={vi.fn()} />, { locale: 'zh-CN' })

    expect(screen.getByRole('heading', { name: '登录控制台' })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: '登录' })).toBeInTheDocument()
    expect(screen.getByLabelText('用户名')).toBeInTheDocument()
  })
})
```

Create `packages/web/src/components/Layout.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it, vi } from 'vitest'
import { Layout } from './Layout'
import { renderWithI18n } from '../test/render'

describe('Layout language switching', () => {
  it('switches navigation labels when the language toggle changes', async () => {
    const user = userEvent.setup()

    await renderWithI18n(
      <Layout
        currentPath="/"
        user={{
          username: 'admin',
          role: 'admin',
          permissions: {
            can_manage_routes: true,
            can_manage_auth: true,
            can_manage_users: true,
            can_view_logs: true,
          },
        }}
        onLogout={vi.fn()}
      >
        <div>content</div>
      </Layout>,
      { locale: 'en' }
    )

    expect(screen.getAllByText('Routes')[0]).toBeInTheDocument()

    await user.click(screen.getByRole('button', { name: '中文' }))

    expect(screen.getAllByText('路由')[0]).toBeInTheDocument()
    expect(localStorage.getItem('auth-gate.locale')).toBe('zh-CN')
  })
})
```

- [ ] **Step 2: Run the tests to verify they fail**

Run:

```bash
npm --prefix packages/web test -- src/pages/LoginPage.test.tsx src/components/Layout.test.tsx
```

Expected:

```text
FAIL because LoginPage and Layout do not use translation hooks or render a language switcher yet.
```

- [ ] **Step 3: Implement the shared language switcher and wire the entry/runtime**

Create `packages/web/src/components/LanguageSwitcher.tsx`:

```tsx
import React from 'react'
import { useTranslation } from 'react-i18next'
import { cn } from '../lib/utils'

const languageOptions = [
  { value: 'en', label: 'EN' },
  { value: 'zh-CN', label: '中文' },
] as const

interface LanguageSwitcherProps {
  className?: string
}

export function LanguageSwitcher({ className }: LanguageSwitcherProps) {
  const { i18n } = useTranslation()

  const active = i18n.resolvedLanguage === 'zh-CN' ? 'zh-CN' : 'en'

  return (
    <div
      className={cn(
        'inline-flex items-center rounded-full border border-[var(--border-default)] bg-[rgba(255,255,255,0.62)] p-1 shadow-[var(--shadow-sm)]',
        className
      )}
      role="group"
      aria-label="Language switcher"
    >
      {languageOptions.map((option) => {
        const isActive = active === option.value
        return (
          <button
            key={option.value}
            type="button"
            onClick={() => void i18n.changeLanguage(option.value)}
            className={cn(
              'rounded-full px-3 py-1.5 text-xs font-semibold transition-colors',
              isActive
                ? 'bg-[linear-gradient(135deg,var(--primary-500),var(--primary-700))] text-white'
                : 'text-[var(--text-muted)] hover:text-[var(--text-primary)]'
            )}
            aria-pressed={isActive}
            aria-label={option.value === 'en' ? 'English' : '中文'}
          >
            {option.label}
          </button>
        )
      })}
    </div>
  )
}
```

Modify `packages/web/src/main.tsx`:

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { I18nextProvider } from 'react-i18next'
import App from './App'
import { i18nPromise } from './lib/i18n'
import './index.css'

if (!window.location.hash) {
  window.location.hash = '/'
}

void i18nPromise.then((i18n) => {
  ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
      <I18nextProvider i18n={i18n}>
        <App />
      </I18nextProvider>
    </React.StrictMode>
  )
})
```

Modify the text usage in `packages/web/src/pages/LoginPage.tsx`:

```tsx
import React from 'react'
import { Lock, Shield, Sparkles, User } from 'lucide-react'
import { useTranslation } from 'react-i18next'
import type { LoginResponse } from '../lib/api/types'
import { ApiError } from '../lib/api/client'
import { Button, Card, Input } from '../components/ui'
import { LanguageSwitcher } from '../components/LanguageSwitcher'

export function LoginPage({ onLogin }: LoginPageProps) {
  const { t } = useTranslation('login')
  const [username, setUsername] = React.useState('')
  const [password, setPassword] = React.useState('')
  const [error, setError] = React.useState('')
  const [loading, setLoading] = React.useState(false)

  const handleSubmit = async (event: React.FormEvent) => {
    event.preventDefault()
    setError('')
    setLoading(true)

    try {
      await onLogin(username, password)
    } catch (err) {
      if (err instanceof ApiError) {
        setError(err.message)
      } else {
        setError(t('errors.network'))
      }
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className="relative flex min-h-screen items-center justify-center overflow-hidden px-4 py-10">
      <div className="absolute right-4 top-4 z-10">
        <LanguageSwitcher />
      </div>
      <div className="grid w-full max-w-6xl items-center gap-6 lg:grid-cols-[1.15fr_0.85fr]">
        <Card tone="inverse" className="relative hidden overflow-hidden rounded-[36px] px-8 py-10 lg:block">
          <div className="relative">
            <div className="eyebrow text-white/72">
              <Sparkles className="h-3.5 w-3.5" />
              {t('hero.eyebrow')}
            </div>
            <h1 className="mt-5 max-w-lg text-5xl font-semibold tracking-[-0.05em] text-white">
              {t('hero.title')}
            </h1>
            <p className="mt-5 max-w-xl text-base leading-7 text-white/78">
              {t('hero.description')}
            </p>
          </div>
        </Card>
        <Card className="mx-auto w-full max-w-lg rounded-[32px] p-6 md:p-8">
          <div className="flex flex-col items-center text-center">
            <div className="eyebrow mt-5">{t('card.eyebrow')}</div>
            <h1 className="mt-3 text-3xl font-semibold tracking-[-0.04em] text-[var(--text-primary)]">
              {t('card.title')}
            </h1>
            <p className="mt-2 max-w-sm text-sm leading-6 text-[var(--text-muted)]">
              {t('card.description')}
            </p>
          </div>
          <form onSubmit={handleSubmit} className="mt-8 space-y-5">
            <Input
              label={t('fields.username')}
              type="text"
              value={username}
              onChange={(event) => setUsername(event.target.value)}
              placeholder={t('fields.usernamePlaceholder')}
              leftIcon={<User className="h-4 w-4" />}
              required
            />
            <Input
              label={t('fields.password')}
              type="password"
              value={password}
              onChange={(event) => setPassword(event.target.value)}
              placeholder={t('fields.passwordPlaceholder')}
              leftIcon={<Lock className="h-4 w-4" />}
              required
            />
            <Button type="submit" className="w-full" size="lg" loading={loading}>
              {t('card.submit')}
            </Button>
          </form>
          <div className="mt-6 rounded-[22px] border border-[var(--border-default)] bg-[rgba(255,255,255,0.52)] px-4 py-3 text-center text-xs text-[var(--text-muted)]">
            {t('card.sessionHint')}
          </div>
        </Card>
      </div>
    </div>
  )
}
```

Modify `packages/web/src/pages/AccessLoginPage.tsx`:

```tsx
import React from 'react'
import { KeyRound, Lock, User } from 'lucide-react'
import { useTranslation } from 'react-i18next'
import { authApi } from '../lib/api/auth'
import { ApiError } from '../lib/api/client'
import { LanguageSwitcher } from '../components/LanguageSwitcher'
import { Button, Card, Input } from '../components/ui'

interface AccessLoginPageProps {
  searchParams: URLSearchParams
}

export function AccessLoginPage({ searchParams }: AccessLoginPageProps) {
  const { t } = useTranslation('accessLogin')
  const routeId = searchParams.get('route_id') || ''
  const routeName = searchParams.get('route_name') || t('protectedRouteFallback')
  const pathPrefix = searchParams.get('path_prefix') || '/'
  const next = searchParams.get('next') || pathPrefix

  const [username, setUsername] = React.useState('')
  const [password, setPassword] = React.useState('')
  const [error, setError] = React.useState('')
  const [loading, setLoading] = React.useState(false)

  const handleSubmit = async (event: React.FormEvent) => {
    event.preventDefault()
    setError('')
    setLoading(true)

    try {
      const result = await authApi.accessLogin({
        route_id: routeId,
        username,
        password,
        next,
      })
      window.location.href = result.next
    } catch (err) {
      if (err instanceof ApiError) {
        setError(err.message)
      } else {
        setError(t('errors.network'))
      }
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className="relative flex min-h-screen items-center justify-center overflow-hidden px-4 py-10">
      <div className="absolute right-4 top-4 z-10">
        <LanguageSwitcher />
      </div>

      <Card className="mx-auto w-full max-w-lg rounded-[32px] p-6 md:p-8">
        <div className="flex flex-col items-center text-center">
          <div className="animate-pulse-glow flex h-16 w-16 items-center justify-center rounded-[24px] bg-[linear-gradient(135deg,var(--primary-500),var(--primary-700))] text-white shadow-[var(--shadow-lg)]">
            <KeyRound className="h-8 w-8" />
          </div>
          <div className="eyebrow mt-5">{t('eyebrow')}</div>
          <h1 className="mt-3 text-3xl font-semibold tracking-[-0.04em] text-[var(--text-primary)]">
            {t('title')}
          </h1>
          <p className="mt-2 max-w-sm text-sm leading-6 text-[var(--text-muted)]">
            {t('description', { routeName })}
          </p>
          <div className="mt-4 rounded-full bg-[rgba(255,255,255,0.6)] px-3 py-1 text-xs font-semibold uppercase tracking-[0.12em] text-[var(--text-muted)]">
            {pathPrefix}
          </div>
        </div>

        <form onSubmit={handleSubmit} className="mt-8 space-y-5">
          {error && (
            <div
              className="rounded-[20px] border border-[rgba(208,71,75,0.16)] bg-[var(--error-light)] px-4 py-3 text-sm font-medium text-[var(--error)]"
              role="alert"
            >
              {error}
            </div>
          )}

          <Input
            label={t('fields.username')}
            type="text"
            value={username}
            onChange={(event) => setUsername(event.target.value)}
            placeholder={t('fields.usernamePlaceholder')}
            leftIcon={<User className="h-4 w-4" />}
            required
          />

          <Input
            label={t('fields.password')}
            type="password"
            value={password}
            onChange={(event) => setPassword(event.target.value)}
            placeholder={t('fields.passwordPlaceholder')}
            leftIcon={<Lock className="h-4 w-4" />}
            required
          />

          <Button type="submit" className="w-full" size="lg" loading={loading} disabled={!routeId}>
            {t('submit')}
          </Button>
        </form>
      </Card>
    </div>
  )
}
```

Modify `packages/web/src/components/Layout.tsx`:

```tsx
import React from 'react'
import {
  FileKey,
  KeyRound,
  LogOut,
  Menu,
  Route as RouteIcon,
  Settings,
  Shield,
  User as UserIcon,
  Users,
  X,
} from 'lucide-react'
import { useTranslation } from 'react-i18next'
import { cn } from '../lib/utils'
import { LanguageSwitcher } from './LanguageSwitcher'

export function Layout({ children, currentPath, user, onLogout }: LayoutProps) {
  const { t } = useTranslation('layout')
  const [sidebarOpen, setSidebarOpen] = React.useState(false)

  const navItems = React.useMemo(() => {
    const items = [
      {
        path: '/',
        icon: RouteIcon,
        label: t('sections.routes.label'),
        description: t('sections.routes.description'),
        visible: user?.permissions?.can_manage_routes !== false,
      },
      {
        path: '/certificates',
        icon: FileKey,
        label: t('sections.certificates.label'),
        description: t('sections.certificates.description'),
        visible: user?.permissions?.can_manage_routes !== false,
      },
      {
        path: '/auth',
        icon: KeyRound,
        label: t('sections.auth.label'),
        description: t('sections.auth.description'),
        visible: user?.permissions?.can_manage_auth !== false,
      },
      {
        path: '/users',
        icon: Users,
        label: t('sections.users.label'),
        description: t('sections.users.description'),
        visible: user?.permissions?.can_manage_users === true,
      },
      {
        path: '/settings',
        icon: Settings,
        label: t('sections.settings.label'),
        description: t('sections.settings.description'),
        visible: true,
      },
    ]

    return items.filter((item) => item.visible)
  }, [t, user])

  const activeItem = navItems.find((item) => item.path === currentPath) ?? navItems[0]
  const closeSidebar = () => setSidebarOpen(false)

  const sidebarContent = (
    <>
      <div className="relative border-b border-[var(--border-soft)] px-5 py-5">
        <div className="flex items-center gap-4">
          <div className="animate-pulse-glow flex h-12 w-12 items-center justify-center rounded-[18px] bg-[linear-gradient(135deg,var(--primary-500),var(--primary-700))] text-white shadow-[var(--shadow-md)]">
            <Shield className="h-6 w-6" aria-hidden="true" />
          </div>
          <div>
            <div className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[var(--text-muted)]">
              {t('brand.name')}
            </div>
            <div className="mt-1 text-lg font-semibold tracking-[-0.03em] text-[var(--text-primary)]">
              {t('brand.controlPlane')}
            </div>
          </div>
        </div>
      </div>

      <div className="px-4 pt-5">
        <div className="rounded-[24px] border border-[var(--border-soft)] bg-[linear-gradient(135deg,rgba(15,143,139,0.12),rgba(255,255,255,0.08))] px-4 py-4">
          <div className="text-[11px] font-semibold uppercase tracking-[0.16em] text-[var(--text-muted)]">
            {t('brand.currentFocus')}
          </div>
          <div className="mt-2 text-lg font-semibold tracking-[-0.02em] text-[var(--text-primary)]">
            {activeItem?.label}
          </div>
          <p className="mt-1 text-sm text-[var(--text-muted)]">{activeItem?.description}</p>
        </div>
        <LanguageSwitcher className="mt-4" />
      </div>

      <nav className="flex-1 space-y-2 px-4 py-5" role="navigation" aria-label={t('navigation.main')}>
        {navItems.map((item) => {
          const isActive = currentPath === item.path
          return (
            <a
              key={item.path}
              href={`#${item.path}`}
              onClick={closeSidebar}
              aria-current={isActive ? 'page' : undefined}
              className={cn(
                'group flex items-center gap-3 rounded-[22px] px-4 py-3 transition-all duration-[var(--duration-normal)]',
                isActive
                  ? 'bg-[linear-gradient(135deg,var(--primary-500),var(--primary-700))] text-white shadow-[var(--shadow-md)]'
                  : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)] hover:text-[var(--text-primary)]'
              )}
            >
              <div
                className={cn(
                  'flex h-11 w-11 items-center justify-center rounded-[16px] transition-colors',
                  isActive ? 'bg-white/18 text-white' : 'bg-[rgba(255,255,255,0.56)] text-[var(--primary-600)]'
                )}
              >
                <item.icon className="h-5 w-5" aria-hidden="true" />
              </div>
              <div className="min-w-0">
                <div className="text-sm font-semibold">{item.label}</div>
                <div className={cn('text-xs', isActive ? 'text-white/78' : 'text-[var(--text-muted)]')}>
                  {item.description}
                </div>
              </div>
            </a>
          )
        })}
      </nav>

      {user && (
        <div className="border-t border-[var(--border-soft)] p-4">
          <div className="rounded-[24px] border border-[var(--border-soft)] bg-[rgba(255,255,255,0.18)] p-4">
            <div className="flex items-center gap-3">
              <div className="flex h-11 w-11 items-center justify-center rounded-[16px] bg-[var(--bg-soft-primary)] text-[var(--primary-600)]">
                <UserIcon className="h-5 w-5" />
              </div>
              <div className="min-w-0 flex-1">
                <p className="truncate text-sm font-semibold text-[var(--text-primary)]">{user.username}</p>
                <p className="text-xs uppercase tracking-[0.12em] text-[var(--text-muted)]">{user.role}</p>
              </div>
              <button
                onClick={onLogout}
                className="flex h-10 w-10 items-center justify-center rounded-full text-[var(--text-muted)] transition-colors hover:bg-[var(--bg-hover)] hover:text-[var(--text-primary)]"
                aria-label={t('user.logout')}
              >
                <LogOut className="h-4 w-4" />
              </button>
            </div>
          </div>
        </div>
      )}
    </>
  )

  return (
    <div className="app-shell-grid min-h-screen bg-transparent">
      {sidebarOpen && (
        <div className="fixed inset-0 z-40 bg-[rgba(15,23,34,0.42)] backdrop-blur-sm md:hidden" onClick={closeSidebar} aria-hidden="true" />
      )}

      <aside
        className={cn(
          'sidebar-sheen glass-panel-strong fixed left-5 top-5 hidden h-[calc(100vh-2.5rem)] flex-col overflow-hidden md:flex',
          'w-[var(--sidebar-width)] z-50'
        )}
      >
        {sidebarContent}
      </aside>

      <aside
        className={cn(
          'glass-panel-strong fixed left-3 top-3 z-50 flex h-[calc(100vh-1.5rem)] w-[var(--sidebar-width)] flex-col overflow-hidden transition-transform duration-[var(--duration-slow)] md:hidden',
          sidebarOpen ? 'translate-x-0' : '-translate-x-[120%]'
        )}
        role="dialog"
        aria-modal="true"
        aria-label={t('navigation.main')}
      >
        <div className="absolute right-4 top-4 z-10">
          <button
            onClick={closeSidebar}
            className="flex h-10 w-10 items-center justify-center rounded-full bg-[rgba(255,255,255,0.44)] text-[var(--text-muted)] transition-colors hover:text-[var(--text-primary)]"
            aria-label={t('navigation.closeMenu')}
          >
            <X className="h-5 w-5" />
          </button>
        </div>
        {sidebarContent}
      </aside>

      <main className="min-h-screen md:ml-[calc(var(--sidebar-width)+2.25rem)]">
        <header className="sticky top-0 z-30 border-b border-[rgba(255,255,255,0.35)] bg-[rgba(246,243,236,0.72)] backdrop-blur-xl md:hidden">
          <div className="flex h-[var(--header-height)] items-center justify-between px-4">
            <button
              onClick={() => setSidebarOpen(true)}
              className="flex h-11 w-11 items-center justify-center rounded-full bg-[rgba(255,255,255,0.72)] text-[var(--text-primary)] shadow-[var(--shadow-sm)]"
              aria-label={t('navigation.openMenu')}
            >
              <Menu className="h-5 w-5" />
            </button>
            <div className="text-center">
              <div className="text-[11px] font-semibold uppercase tracking-[0.16em] text-[var(--text-muted)]">
                {t('brand.name')}
              </div>
              <div className="text-sm font-semibold text-[var(--text-primary)]">{activeItem?.label}</div>
            </div>
            <div className="flex h-11 w-11 items-center justify-center rounded-full bg-[linear-gradient(135deg,var(--primary-500),var(--primary-700))] text-white shadow-[var(--shadow-sm)]">
              <Shield className="h-5 w-5" />
            </div>
          </div>
        </header>

        <div className="mx-auto flex min-h-screen w-full max-w-[calc(var(--page-max-width)+4rem)] flex-col px-4 pb-[calc(var(--bottom-nav-height)+1rem)] pt-5 md:px-8 md:pb-10 md:pt-5">
          {children}
        </div>

        <nav
          className="glass-panel fixed bottom-3 left-3 right-3 z-30 flex rounded-[28px] border border-[var(--border-soft)] px-2 py-2 md:hidden"
          role="navigation"
          aria-label={t('navigation.mobile')}
        >
          {navItems.map((item) => {
            const isActive = currentPath === item.path
            return (
              <a
                key={item.path}
                href={`#${item.path}`}
                aria-current={isActive ? 'page' : undefined}
                className={cn(
                  'flex flex-1 flex-col items-center justify-center gap-1 rounded-[22px] px-2 py-2 text-[11px] font-semibold transition-all',
                  isActive
                    ? 'bg-[linear-gradient(135deg,var(--primary-500),var(--primary-700))] text-white shadow-[var(--shadow-sm)]'
                    : 'text-[var(--text-muted)]'
                )}
              >
                <item.icon className="h-4 w-4" />
                <span>{item.label}</span>
              </a>
            )
          })}
        </nav>
      </main>
    </div>
  )
}
```

- [ ] **Step 4: Run the tests to verify they pass**

Run:

```bash
npm --prefix packages/web test -- src/pages/LoginPage.test.tsx src/components/Layout.test.tsx
```

Expected:

```text
PASS src/pages/LoginPage.test.tsx
PASS src/components/Layout.test.tsx
```

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/main.tsx packages/web/src/components/LanguageSwitcher.tsx packages/web/src/components/Layout.tsx packages/web/src/pages/LoginPage.tsx packages/web/src/pages/AccessLoginPage.tsx packages/web/src/pages/LoginPage.test.tsx packages/web/src/components/Layout.test.tsx packages/web/src/lib/i18n/resources
git commit -m "feat(web): add language switching for login and layout"
```

## Task 3: Translate Shared Admin UI Primitives

**Files:**
- Create: `packages/web/src/components/DataTable.test.tsx`
- Modify: `packages/web/src/components/DataTable.tsx`
- Modify: `packages/web/src/components/PageHeader.tsx`
- Modify: `packages/web/src/components/ui/Table.tsx`
- Modify: `packages/web/src/components/ui/Modal.tsx`
- Modify: `packages/web/src/lib/i18n/resources/common.ts`
- Test: `packages/web/src/components/DataTable.test.tsx`

- [ ] **Step 1: Write the failing test for shared table and action labels**

Create `packages/web/src/components/DataTable.test.tsx`:

```tsx
import { screen } from '@testing-library/react'
import { describe, expect, it, vi } from 'vitest'
import { DataTable } from './DataTable'
import { renderWithI18n } from '../test/render'

describe('DataTable i18n', () => {
  it('renders translated empty and action labels', async () => {
    await renderWithI18n(
      <DataTable
        columns={[{ key: 'name', header: 'Name' }]}
        data={[]}
        onEdit={vi.fn()}
        onDelete={vi.fn()}
      />,
      { locale: 'zh-CN' }
    )

    expect(screen.getByText('暂无数据')).toBeInTheDocument()
    expect(screen.getByText('操作')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run:

```bash
npm --prefix packages/web test -- src/components/DataTable.test.tsx
```

Expected:

```text
FAIL because DataTable and Table still hardcode English defaults and action labels.
```

- [ ] **Step 3: Translate shared component-owned copy**

Modify `packages/web/src/components/DataTable.tsx`:

```tsx
import React from 'react'
import { Pencil, Trash2 } from 'lucide-react'
import { useTranslation } from 'react-i18next'
import {
  EmptyRow,
  MobileCardList,
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from './ui'

export function DataTable<T extends { id: string }>({
  columns,
  data,
  onEdit,
  onDelete,
  extraActions,
  renderMobileCard,
  emptyMessage,
}: DataTableProps<T>) {
  const { t } = useTranslation('common')
  const resolvedEmptyMessage = emptyMessage ?? t('table.noData')

  const mobileCardRenderer =
    renderMobileCard ||
    ((row: T) => (
      <div className="space-y-4">
        <div className="space-y-3">
          {columns.filter((col) => !col.hideOnMobile).map((col) => {
            const value = (row as any)[col.key]
            return (
              <div key={col.key as string} className="flex items-start justify-between gap-4">
                <span className="text-[11px] font-semibold uppercase tracking-[0.14em] text-[var(--text-muted)]">
                  {col.header}
                </span>
                <span className="min-w-0 text-right text-sm text-[var(--text-primary)]">
                  {col.render ? col.render(value, row) : String(value ?? '-')}
                </span>
              </div>
            )
          })}
        </div>
        {(onEdit || onDelete || extraActions) && (
          <div className="flex justify-end gap-2 border-t border-[var(--border-default)] pt-3">
            {extraActions && <div className="mr-2 flex items-center">{extraActions(row)}</div>}
            {onEdit && (
              <ActionButton label={t('actions.edit')} onClick={() => onEdit(row)}>
                <Pencil className="h-4 w-4" />
              </ActionButton>
            )}
            {onDelete && (
              <ActionButton label={t('actions.delete')} danger onClick={() => onDelete(row)}>
                <Trash2 className="h-4 w-4" />
              </ActionButton>
            )}
          </div>
        )}
      </div>
    ))

  return (
    <>
      <div className="hidden md:block">
        <Table>
          <TableHeader>
            <TableRow>
              {columns.map((col) => (
                <TableHead key={col.key as string} className={col.className}>
                  {col.header}
                </TableHead>
              ))}
              {(onEdit || onDelete || extraActions) && (
                <TableHead className="w-40 text-right">{t('table.actions')}</TableHead>
              )}
            </TableRow>
          </TableHeader>
          <TableBody>
            {data.length === 0 ? (
              <EmptyRow
                colSpan={columns.length + ((onEdit || onDelete || extraActions) ? 1 : 0)}
                message={resolvedEmptyMessage}
              />
            ) : (
              data.map((row) => (
                <TableRow key={row.id}>
                  {columns.map((col) => {
                    const value = col.key.toString().includes('.')
                      ? (row as any)[col.key]
                      : row[col.key as keyof T]
                    return (
                      <TableCell key={col.key as string} className={col.className}>
                        {col.render ? col.render(value, row) : String(value ?? '-')}
                      </TableCell>
                    )
                  })}
                  {(onEdit || onDelete || extraActions) && (
                    <TableCell className="text-right">
                      <div className="flex justify-end gap-2">
                        {extraActions && <div className="flex items-center">{extraActions(row)}</div>}
                        {onEdit && (
                          <ActionButton label={t('actions.edit')} onClick={() => onEdit(row)}>
                            <Pencil className="h-4 w-4" />
                          </ActionButton>
                        )}
                        {onDelete && (
                          <ActionButton label={t('actions.delete')} danger onClick={() => onDelete(row)}>
                            <Trash2 className="h-4 w-4" />
                          </ActionButton>
                        )}
                      </div>
                    </TableCell>
                  )}
                </TableRow>
              ))
            )}
          </TableBody>
        </Table>
      </div>

      <div className="md:hidden">
        <MobileCardList data={data} renderCard={mobileCardRenderer} emptyMessage={resolvedEmptyMessage} />
      </div>
    </>
  )
}
```

Modify `packages/web/src/components/PageHeader.tsx`:

```tsx
import React from 'react'
import { useTranslation } from 'react-i18next'
import { cn } from '../lib/utils'

export function PageHeader({
  title,
  description,
  eyebrow,
  action,
  meta,
}: PageHeaderProps) {
  const { t } = useTranslation('layout')
  const resolvedEyebrow = eyebrow ?? t('brand.controlPlane')

  return (
    <div className="mb-6 rounded-[32px] border border-[var(--border-soft)] bg-[linear-gradient(135deg,rgba(255,255,255,0.78),rgba(255,255,255,0.42))] px-5 py-6 shadow-[var(--shadow-md)] backdrop-blur-xl md:mb-8 md:px-7 md:py-7">
      <div className="flex flex-col gap-5 md:flex-row md:items-end md:justify-between">
        <div className="min-w-0">
          <div className="eyebrow">
            <span className="inline-flex h-2.5 w-2.5 rounded-full bg-[var(--primary-500)] animate-pulse-glow" />
            {resolvedEyebrow}
          </div>
```

Modify `packages/web/src/components/ui/Table.tsx`:

```tsx
interface EmptyRowProps {
  colSpan: number
  message: string
}

export function EmptyRow({ colSpan, message }: EmptyRowProps) {
  return (
    <tr>
      <td colSpan={colSpan} className="h-36 px-4 text-center text-sm text-[var(--text-muted)]" role="status">
        {message}
      </td>
    </tr>
  )
}

interface MobileCardListProps<T> {
  data: T[]
  renderCard: (item: T) => React.ReactNode
  emptyMessage: string
}
```

Modify `packages/web/src/components/ui/Modal.tsx`:

```tsx
import React, { useEffect, useRef } from 'react'
import { X } from 'lucide-react'
import { useTranslation } from 'react-i18next'
import { cn } from '../../lib/utils'

type ModalSize = 'sm' | 'md' | 'lg'

interface ModalProps {
  open: boolean
  onClose: () => void
  modalSize?: ModalSize
  title?: string
  children: React.ReactNode
}

const sizeStyles: Record<ModalSize, string> = {
  sm: 'md:max-w-[440px]',
  md: 'md:max-w-[620px]',
  lg: 'md:max-w-[820px]',
}

export function Modal({
  open,
  onClose,
  modalSize = 'md',
  title,
  children,
}: ModalProps) {
  const { t } = useTranslation('common')
  const previousActiveElement = useRef<HTMLElement | null>(null)
  const panelRef = useRef<HTMLDivElement | null>(null)

  useEffect(() => {
    if (open) {
      previousActiveElement.current = document.activeElement as HTMLElement
      document.body.style.overflow = 'hidden'
      panelRef.current?.focus()
      return
    }

    document.body.style.overflow = ''
    previousActiveElement.current?.focus()
  }, [open])

  useEffect(() => {
    const handleEscape = (event: KeyboardEvent) => {
      if (event.key === 'Escape' && open) {
        onClose()
      }
    }

    document.addEventListener('keydown', handleEscape)
    return () => {
      document.body.style.overflow = ''
      document.removeEventListener('keydown', handleEscape)
    }
  }, [open, onClose])

  if (!open) return null

  return (
    <div
      role="dialog"
      aria-modal="true"
      aria-labelledby={title ? 'modal-title' : undefined}
      className="fixed inset-0 z-[var(--z-modal-backdrop)]"
      onClick={onClose}
    >
      <div className="absolute inset-0 bg-[rgba(15,23,34,0.52)] backdrop-blur-md" aria-hidden="true" />
      <div className="flex min-h-screen items-end justify-center p-0 md:items-center md:p-6">
        <div
          ref={panelRef}
          className={cn(
            'relative w-full rounded-t-[30px] border border-white/10 bg-[var(--bg-elevated)] shadow-[var(--shadow-xl)] outline-none animate-modal-enter',
            'max-h-[92vh] overflow-y-auto md:rounded-[30px]',
            sizeStyles[modalSize]
          )}
          onClick={(event) => event.stopPropagation()}
          tabIndex={-1}
        >
          {title && (
            <div className="sticky top-0 z-10 flex items-center justify-between border-b border-[var(--border-default)] bg-[var(--bg-elevated)] px-6 py-5 backdrop-blur-xl">
              <h2 id="modal-title" className="text-xl font-semibold tracking-[-0.02em] text-[var(--text-primary)]">
                {title}
              </h2>
              <button
                onClick={onClose}
                className="flex h-10 w-10 items-center justify-center rounded-full text-[var(--text-muted)] transition-colors hover:bg-[var(--bg-hover)] hover:text-[var(--text-primary)]"
                aria-label={t('actions.closeModal')}
              >
                <X className="h-4 w-4" />
              </button>
            </div>
          )}
          <div className="p-5 md:p-6">{children}</div>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run:

```bash
npm --prefix packages/web test -- src/components/DataTable.test.tsx
```

Expected:

```text
PASS src/components/DataTable.test.tsx
```

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/components/DataTable.tsx packages/web/src/components/PageHeader.tsx packages/web/src/components/ui/Table.tsx packages/web/src/components/ui/Modal.tsx packages/web/src/components/DataTable.test.tsx packages/web/src/lib/i18n/resources/common.ts
git commit -m "feat(web): translate shared admin ui primitives"
```

## Task 4: Translate Routes, Auth Rules, Users, Settings, and Their Forms

**Files:**
- Create: `packages/web/src/lib/i18n/resources/routes.ts`
- Create: `packages/web/src/lib/i18n/resources/authRules.ts`
- Create: `packages/web/src/lib/i18n/resources/users.ts`
- Create: `packages/web/src/lib/i18n/resources/settings.ts`
- Modify: `packages/web/src/lib/i18n/resources/index.ts`
- Modify: `packages/web/src/pages/RoutesPage.tsx`
- Modify: `packages/web/src/pages/AuthRulesPage.tsx`
- Modify: `packages/web/src/pages/UsersPage.tsx`
- Modify: `packages/web/src/pages/SettingsPage.tsx`
- Modify: `packages/web/src/components/RouteForm.tsx`
- Modify: `packages/web/src/components/AuthRuleForm.tsx`
- Modify: `packages/web/src/components/UserForm.tsx`
- Test: reuse existing `src/components/Layout.test.tsx` and `src/components/DataTable.test.tsx`

- [ ] **Step 1: Write a failing page-level expectation for one translated admin page**

Add this test case to `packages/web/src/components/Layout.test.tsx`:

```tsx
  it('renders translated navigation labels in zh-CN when locale is seeded', async () => {
    await renderWithI18n(
      <Layout
        currentPath="/"
        user={{
          username: 'admin',
          role: 'admin',
          permissions: {
            can_manage_routes: true,
            can_manage_auth: true,
            can_manage_users: true,
            can_view_logs: true,
          },
        }}
      >
        <div>content</div>
      </Layout>,
      { locale: 'zh-CN' }
    )

    expect(screen.getAllByText('路由')[0]).toBeInTheDocument()
    expect(screen.getAllByText('设置')[0]).toBeInTheDocument()
  })
```

- [ ] **Step 2: Run the test to verify it fails for untranslated page-owned strings**

Run:

```bash
npm --prefix packages/web test -- src/components/Layout.test.tsx
```

Expected:

```text
FAIL while some labels or descriptions still come from hardcoded page/component strings.
```

- [ ] **Step 3: Add page namespace resources**

Create `packages/web/src/lib/i18n/resources/routes.ts`:

```ts
export const routesEn = {
  page: {
    eyebrow: 'Traffic Orchestration',
    title: 'Routes',
    description: 'Define how inbound requests are matched and forwarded across your services.',
    badge: 'Routing Matrix',
    configuredEntries: '{{count}} configured route entries',
    addRoute: 'Add Route',
    loading: 'Loading routes...',
    errorTitle: 'Route operation failed',
    totalRoutes: 'Total Routes',
    totalRoutesHint: 'All configured forwarding rules.',
    activeRoutes: 'Active Routes',
    activeRoutesHint: 'Enabled rules currently available to runtime.',
    hostCoverage: 'Host Coverage',
    liveSnapshot: 'Live config snapshot',
    directoryEyebrow: 'Route Directory',
    directoryTitle: 'Forwarding topology',
    directoryDescription: 'Inspect route health, backend targets, and match precedence at a glance.',
    emptyTitle: 'No routes configured',
    emptyDescription: 'Create your first route to start forwarding traffic into protected backend services.',
    createFirst: 'Create First Route',
    addModalTitle: 'Add Route',
    editModalTitle: 'Edit Route',
    deleteConfirm: 'Delete route "{{name}}"?',
    untitled: 'Untitled Route',
    allHosts: 'all hosts',
    wildcard: 'Wildcard',
    active: 'Active',
    disabled: 'Disabled',
  },
  table: {
    route: 'Route',
    hostMatch: 'Host Match',
    backendTarget: 'Backend Target',
    priority: 'Priority',
    status: 'Status',
  },
  form: {
    identityEyebrow: 'Route Identity',
    identityDescription: 'Define the matching scope and backend target for this route.',
    name: 'Name',
    namePlaceholder: 'Billing API',
    host: 'Host',
    hostPlaceholder: 'api.example.com',
    hostHint: 'Leave empty to match every host.',
    pathPrefix: 'Path Prefix',
    pathPrefixPlaceholder: '/billing or empty to match all',
    pathPrefixHint: 'Leave empty to match all paths.',
    backend: 'Backend',
    backendPlaceholder: 'http://127.0.0.1:3000',
    priority: 'Priority',
    priorityHint: 'Higher values win when multiple routes can match the same request.',
    pathMatchMode: 'Path Match Mode',
    rewriteTarget: 'Rewrite Target',
    rewriteTargetPlaceholder: '/new/$1',
    rewriteTargetHint: 'Use $1, $2 for regex capture groups. $& for full match.',
    redirectCode: 'Redirect Code',
    noRedirect: 'No redirect',
    movedPermanently: '301 Moved Permanently',
    foundTemporary: '302 Found (Temporary)',
    redirectHint: 'Use with Rewrite Target for external redirects (e.g. HTTP → HTTPS).',
    stripPrefix: 'Strip Prefix',
    stripPrefixDescription: 'Remove the incoming prefix before forwarding traffic to the backend.',
    enabled: 'Enabled',
    enabledDescription: 'Keep the route active and available to the runtime router.',
    update: 'Update Route',
    create: 'Create Route',
  },
} as const

export const routesZhCN = {
  page: {
    eyebrow: '流量编排',
    title: '路由',
    description: '定义入站请求如何在你的服务之间匹配并转发。',
    badge: '路由矩阵',
    configuredEntries: '{{count}} 条已配置路由',
    addRoute: '新增路由',
    loading: '正在加载路由...',
    errorTitle: '路由操作失败',
    totalRoutes: '路由总数',
    totalRoutesHint: '所有已配置的转发规则。',
    activeRoutes: '启用路由',
    activeRoutesHint: '当前可供运行时使用的启用规则。',
    hostCoverage: 'Host 覆盖',
    liveSnapshot: '实时配置快照',
    directoryEyebrow: '路由目录',
    directoryTitle: '转发拓扑',
    directoryDescription: '快速查看路由状态、后端目标和匹配优先级。',
    emptyTitle: '尚未配置路由',
    emptyDescription: '创建第一条路由，把流量转发到受保护的后端服务。',
    createFirst: '创建第一条路由',
    addModalTitle: '新增路由',
    editModalTitle: '编辑路由',
    deleteConfirm: '确认删除路由“{{name}}”？',
    untitled: '未命名路由',
    allHosts: '所有主机',
    wildcard: '通配',
    active: '启用',
    disabled: '禁用',
  },
  table: {
    route: '路由',
    hostMatch: 'Host 匹配',
    backendTarget: '后端目标',
    priority: '优先级',
    status: '状态',
  },
  form: {
    identityEyebrow: '路由标识',
    identityDescription: '定义该路由的匹配范围和后端目标。',
    name: '名称',
    namePlaceholder: '账单 API',
    host: 'Host',
    hostPlaceholder: 'api.example.com',
    hostHint: '留空则匹配所有 Host。',
    pathPrefix: '路径前缀',
    pathPrefixPlaceholder: '/billing 或留空匹配全部',
    pathPrefixHint: '留空则匹配所有路径。',
    backend: '后端地址',
    backendPlaceholder: 'http://127.0.0.1:3000',
    priority: '优先级',
    priorityHint: '当多个路由都能匹配请求时，优先级更高者生效。',
    pathMatchMode: '路径匹配模式',
    rewriteTarget: '重写目标',
    rewriteTargetPlaceholder: '/new/$1',
    rewriteTargetHint: '正则捕获组使用 $1、$2，完整匹配使用 $&。',
    redirectCode: '重定向状态码',
    noRedirect: '不重定向',
    movedPermanently: '301 永久移动',
    foundTemporary: '302 临时跳转',
    redirectHint: '与重写目标配合使用外部重定向，例如 HTTP → HTTPS。',
    stripPrefix: '去除前缀',
    stripPrefixDescription: '转发到后端前移除传入请求的路径前缀。',
    enabled: '启用',
    enabledDescription: '保持该路由对运行时路由器可见且可用。',
    update: '更新路由',
    create: '创建路由',
  },
} as const
```

Create `packages/web/src/lib/i18n/resources/authRules.ts`:

```ts
export const authRulesEn = {
  page: {
    eyebrow: 'Policy Enforcement',
    title: 'Auth Rules',
    description: 'Attach route-level credentials and keep sensitive paths behind explicit verification rules.',
    badge: 'Security Policies',
    activeRuleDefinitions: '{{count}} active rule definitions',
    addRule: 'Add Rule',
    loading: 'Loading auth rules...',
    errorTitle: 'Auth rule operation failed',
    protectedRoutes: 'Protected Routes',
    protectedRoutesHint: 'Unique routes with attached auth policies.',
    apiKeyRules: 'API Key Rules',
    apiKeyRulesHint: 'Header-based key validation rules.',
    bearerAndBasic: 'Bearer + Basic',
    bearerAndBasicHint: 'Token and credential verification rules.',
    directoryEyebrow: 'Policy Directory',
    directoryTitle: 'Authentication matrix',
    directoryDescription: 'Audit which routes are protected and how credentials are validated before traffic reaches upstream services.',
    emptyTitle: 'No auth rules configured',
    emptyDescription: 'Add a rule to require API keys, bearer tokens, basic auth, or gateway-managed login before requests are forwarded.',
    createFirst: 'Create First Rule',
    addModalTitle: 'Add Auth Rule',
    editModalTitle: 'Edit Auth Rule',
    deleteConfirm: 'Delete this auth rule?',
    headerBasedKey: 'Header based key',
    bearerValidation: 'Bearer token validation',
    basicCredentials: 'Basic credentials',
    gatewayLogin: 'Gateway-managed user login',
    noCredentials: 'No credential requirement',
  },
  table: {
    protectedRoute: 'Protected Route',
    authType: 'Auth Type',
    credentialMapping: 'Credential Mapping',
  },
  form: {
    scopeEyebrow: 'Policy Scope',
    scopeDescription: 'Attach an auth policy to a route and provide the credentials it should validate.',
    route: 'Route',
    type: 'Type',
    headerName: 'Header Name',
    secret: 'Secret',
    jwtSecret: 'JWT Secret',
    jwtSecretHint: 'Current implementation validates HMAC-signed JWTs with this shared secret.',
    username: 'Username',
    usernamePlaceholder: 'service-user',
    password: 'Password',
    passwordPlaceholder: 'service-password',
    loginMode: 'Login Mode',
    loginModeHint: "Uses Auth Gate's route access login page and the shared user directory.",
    update: 'Update Rule',
    create: 'Create Rule',
  },
  types: {
    none: 'None',
    apikey: 'API Key',
    bearer: 'Bearer Token',
    basic: 'Basic Auth',
    gateway: 'Gateway Login',
    gatewayForm: 'Gateway Form Login',
  },
} as const

export const authRulesZhCN = {
  page: {
    eyebrow: '策略执行',
    title: '鉴权规则',
    description: '把路由级凭证校验附加到受保护路径上，确保上游服务之前就完成身份验证。',
    badge: '安全策略',
    activeRuleDefinitions: '{{count}} 条启用规则',
    addRule: '新增规则',
    loading: '正在加载鉴权规则...',
    errorTitle: '鉴权规则操作失败',
    protectedRoutes: '受保护路由',
    protectedRoutesHint: '已附加鉴权策略的唯一路由数。',
    apiKeyRules: 'API Key 规则',
    apiKeyRulesHint: '基于 Header 的密钥校验规则。',
    bearerAndBasic: 'Bearer + Basic',
    bearerAndBasicHint: 'Token 与账号密码校验规则。',
    directoryEyebrow: '策略目录',
    directoryTitle: '认证矩阵',
    directoryDescription: '审查哪些路由受保护，以及流量进入上游前会执行何种凭证校验。',
    emptyTitle: '尚未配置鉴权规则',
    emptyDescription: '新增规则后，请求在转发前可被要求提供 API Key、Bearer Token、Basic Auth 或网关托管登录。',
    createFirst: '创建第一条规则',
    addModalTitle: '新增鉴权规则',
    editModalTitle: '编辑鉴权规则',
    deleteConfirm: '确认删除这条鉴权规则？',
    headerBasedKey: '基于 Header 的密钥校验',
    bearerValidation: 'Bearer Token 校验',
    basicCredentials: 'Basic Auth 凭证',
    gatewayLogin: '网关托管登录',
    noCredentials: '无凭证要求',
  },
  table: {
    protectedRoute: '受保护路由',
    authType: '鉴权类型',
    credentialMapping: '凭证映射',
  },
  form: {
    scopeEyebrow: '策略范围',
    scopeDescription: '将鉴权策略绑定到某条路由，并提供要校验的凭证信息。',
    route: '路由',
    type: '类型',
    headerName: 'Header 名称',
    secret: '密钥',
    jwtSecret: 'JWT 密钥',
    jwtSecretHint: '当前实现使用这个共享密钥校验 HMAC 签名的 JWT。',
    username: '用户名',
    usernamePlaceholder: 'service-user',
    password: '密码',
    passwordPlaceholder: 'service-password',
    loginMode: '登录方式',
    loginModeHint: '使用 Auth Gate 的路由访问登录页以及共享用户目录。',
    update: '更新规则',
    create: '创建规则',
  },
  types: {
    none: '无',
    apikey: 'API Key',
    bearer: 'Bearer Token',
    basic: 'Basic Auth',
    gateway: '网关登录',
    gatewayForm: '网关表单登录',
  },
} as const
```

Create `packages/web/src/lib/i18n/resources/users.ts`:

```ts
export const usersEn = {
  page: {
    eyebrow: 'Identity Directory',
    title: 'Users',
    description: 'Manage control-plane operators and route-access members from a shared account directory.',
    badge: 'Access Model',
    managedUsers: '{{count}} managed users',
    addUser: 'Add User',
    loading: 'Loading users...',
    errorTitle: 'User operation failed',
    enabledUsers: 'Enabled Users',
    enabledUsersHint: 'Accounts that can currently authenticate.',
    routeMembers: 'Route Members',
    routeMembersHint: 'Users intended for gateway-managed route access.',
    operators: 'Operators',
    operatorsHint: 'Control-plane capable accounts.',
    directoryEyebrow: 'Directory',
    directoryTitle: 'User access registry',
    directoryDescription: 'Assign control-plane roles separately from route access permissions so gateway users do not automatically become admins.',
    emptyTitle: 'No users configured',
    emptyDescription: 'Create your first user to start assigning control-plane or route access.',
    createFirst: 'Create First User',
    addModalTitle: 'Add User',
    editModalTitle: 'Edit User',
    deleteConfirm: 'Delete user "{{username}}"?',
    assignedRoutes: '{{count}} assigned routes',
    allRoutes: 'All routes',
    noAssignedRoutes: 'No assigned routes',
    routeCount_one: '{{count}} route',
    routeCount_other: '{{count}} routes',
    enabled: 'Enabled',
    disabled: 'Disabled',
  },
  table: {
    user: 'User',
    role: 'Role',
    status: 'Status',
    accessScope: 'Access Scope',
  },
  form: {
    identityEyebrow: 'Identity',
    identityDescription: 'Create a control-plane or route-access account from the shared user directory.',
    username: 'Username',
    role: 'Role',
    memberHint: 'Members do not get control-plane access.',
    operatorHint: 'Viewer/editor/admin can log into the control plane.',
    password: 'Password',
    passwordPlaceholder: 'Enter password',
    newPassword: 'New Password',
    newPasswordPlaceholder: 'Leave blank to keep existing password',
    newPasswordHint: 'Only fill this when you want to rotate the password.',
    enabled: 'Enabled',
    enabledDescription: 'Disabled users cannot access the control plane or protected routes.',
    accessEyebrow: 'Route Access',
    accessDescription: 'Assign which routes this account may access when a route uses gateway-managed login.',
    noRoutes: 'No routes available yet. Create routes before assigning access.',
    allHosts: 'all hosts',
    update: 'Update User',
    create: 'Create User',
  },
  roles: {
    member: 'Member',
    viewer: 'Viewer',
    editor: 'Editor',
    admin: 'Admin',
  },
} as const

export const usersZhCN = {
  page: {
    eyebrow: '身份目录',
    title: '用户',
    description: '在同一个账号目录中管理控制台操作员和路由访问成员。',
    badge: '访问模型',
    managedUsers: '{{count}} 个受管用户',
    addUser: '新增用户',
    loading: '正在加载用户...',
    errorTitle: '用户操作失败',
    enabledUsers: '启用用户',
    enabledUsersHint: '当前可成功登录的账号数。',
    routeMembers: '路由成员',
    routeMembersHint: '用于网关托管路由访问的用户。',
    operators: '操作员',
    operatorsHint: '具备控制台访问能力的账号。',
    directoryEyebrow: '目录',
    directoryTitle: '用户访问注册表',
    directoryDescription: '把控制台角色和路由访问权限分开配置，避免网关用户自动获得管理员权限。',
    emptyTitle: '尚未配置用户',
    emptyDescription: '创建第一位用户，开始分配控制台或路由访问权限。',
    createFirst: '创建第一个用户',
    addModalTitle: '新增用户',
    editModalTitle: '编辑用户',
    deleteConfirm: '确认删除用户“{{username}}”？',
    assignedRoutes: '已分配 {{count}} 条路由',
    allRoutes: '全部路由',
    noAssignedRoutes: '未分配路由',
    routeCount_one: '{{count}} 条路由',
    routeCount_other: '{{count}} 条路由',
    enabled: '启用',
    disabled: '禁用',
  },
  table: {
    user: '用户',
    role: '角色',
    status: '状态',
    accessScope: '访问范围',
  },
  form: {
    identityEyebrow: '身份信息',
    identityDescription: '在共享用户目录中创建控制台或路由访问账号。',
    username: '用户名',
    role: '角色',
    memberHint: '成员角色不具备控制台访问权限。',
    operatorHint: 'viewer、editor、admin 可登录控制台。',
    password: '密码',
    passwordPlaceholder: '输入密码',
    newPassword: '新密码',
    newPasswordPlaceholder: '留空则保持现有密码',
    newPasswordHint: '只有在需要轮换密码时才填写这里。',
    enabled: '启用',
    enabledDescription: '被禁用的用户不能访问控制台或受保护路由。',
    accessEyebrow: '路由访问',
    accessDescription: '当路由使用网关托管登录时，指定该账号可访问哪些路由。',
    noRoutes: '当前没有可分配的路由。请先创建路由。',
    allHosts: '所有主机',
    update: '更新用户',
    create: '创建用户',
  },
  roles: {
    member: '成员',
    viewer: '查看者',
    editor: '编辑者',
    admin: '管理员',
  },
} as const
```

Create `packages/web/src/lib/i18n/resources/settings.ts`:

```ts
export const settingsEn = {
  page: {
    eyebrow: 'Runtime Operations',
    title: 'Settings',
    description: 'Handle runtime reloads, inspect storage assumptions, and review operational safeguards.',
    successTitle: 'Configuration reloaded',
    errorTitle: 'Settings operation failed',
    reloadAccess: 'Reload Access',
    reloadGranted: 'Granted',
    reloadRestricted: 'Restricted',
    reloadAccessHint: 'Derived from current session permissions.',
    storage: 'Storage',
    storageHint: 'State persists in the local gateway database.',
    version: 'Version',
    versionHint: 'Current control plane release indicator.',
    runtimeEyebrow: 'Runtime Control',
    runtimeTitle: 'Configuration reload',
    runtimeDescription: 'Apply manual changes from the database into the running gateway without restarting the process.',
    reloadCardTitle: 'Push config to runtime',
    reloadCardDescription: 'Reload configuration after route or auth changes so the runtime compiler can rebuild its active state.',
    reloadButton: 'Reload Config',
    reloadingButton: 'Reloading...',
    reloadFallback: 'Failed to reload configuration',
    storageTitle: 'Storage',
    storageSubtitle: 'Persistent control plane state.',
    storageBody: 'Data is stored in SQLite at {{path}}.',
    securityTitle: 'Security',
    securitySubtitle: 'Deployment checklist.',
    securityBody: 'Configure a strong auth.jwt_secret before production deployment. Ephemeral JWT secrets should remain development-only.',
    aboutTitle: 'About',
    aboutSubtitle: 'Runtime and product context.',
    aboutProduct: 'Auth Gate control plane',
    aboutVersion: 'Version: {{version}}',
    aboutRole: 'Active role: {{role}}',
  },
} as const

export const settingsZhCN = {
  page: {
    eyebrow: '运行时操作',
    title: '设置',
    description: '处理运行时重载、查看存储信息，并审查运维安全注意事项。',
    successTitle: '配置已重载',
    errorTitle: '设置操作失败',
    reloadAccess: '重载权限',
    reloadGranted: '已授予',
    reloadRestricted: '受限',
    reloadAccessHint: '根据当前会话权限计算。',
    storage: '存储',
    storageHint: '状态保存在本地网关数据库中。',
    version: '版本',
    versionHint: '当前控制台发布版本标识。',
    runtimeEyebrow: '运行时控制',
    runtimeTitle: '配置重载',
    runtimeDescription: '无需重启进程，即可把数据库中的手动变更应用到正在运行的网关。',
    reloadCardTitle: '推送配置到运行时',
    reloadCardDescription: '在修改路由或鉴权规则后执行重载，让运行时编译器重建当前生效状态。',
    reloadButton: '重载配置',
    reloadingButton: '重载中...',
    reloadFallback: '重载配置失败',
    storageTitle: '存储',
    storageSubtitle: '持久化的控制台状态。',
    storageBody: '数据保存在 SQLite 文件 {{path}} 中。',
    securityTitle: '安全',
    securitySubtitle: '部署检查清单。',
    securityBody: '生产环境部署前请配置强随机的 auth.jwt_secret。临时 JWT 密钥仅应保留在开发环境。',
    aboutTitle: '关于',
    aboutSubtitle: '运行时与产品上下文。',
    aboutProduct: 'Auth Gate 控制台',
    aboutVersion: '版本：{{version}}',
    aboutRole: '当前角色：{{role}}',
  },
} as const
```

- [ ] **Step 4: Apply translations to the pages and forms**

Use `useTranslation` in each page/form and replace hardcoded strings with namespace keys:

```tsx
// RoutesPage.tsx pattern
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('routes')

if (loading) {
  return (
    <div className="flex h-64 items-center justify-center">
      <div className="flex items-center gap-3 text-[var(--text-muted)]">
        <div className="h-8 w-8 animate-spin rounded-full border-2 border-[var(--primary-500)] border-t-transparent" />
        {t('page.loading')}
      </div>
    </div>
  )
}

if (!confirm(t('page.deleteConfirm', { name: route.name || route.path_prefix }))) return

const columns = [
  {
    key: 'name',
    header: t('table.route'),
    render: (value: string, row: Route) => (
      <div className="min-w-0">
        <div className="font-semibold text-[var(--text-primary)]">{value || t('page.untitled')}</div>
        <div className="mt-1 text-xs text-[var(--text-muted)]">{row.path_prefix}</div>
      </div>
    ),
  },
  {
    key: 'host',
    header: t('table.hostMatch'),
    render: (value: string) => <span className="app-code">{value || t('page.allHosts')}</span>,
  },
]

<PageHeader
  eyebrow={t('page.eyebrow')}
  title={t('page.title')}
  description={t('page.description')}
  meta={
    <>
      <Badge variant="primary">{t('page.badge')}</Badge>
      <span className="text-sm text-[var(--text-muted)]">
        {t('page.configuredEntries', { count: routes.length })}
      </span>
    </>
  }
  action={
    canManageRoutes ? (
      <Button icon={<Plus className="h-4 w-4" />} onClick={() => { setEditingRoute(null); setShowForm(true) }}>
        {t('page.addRoute')}
      </Button>
    ) : null
  }
/>
```

```tsx
// RouteForm.tsx pattern
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('routes')

<Input label={t('form.name')} placeholder={t('form.namePlaceholder')} />
<Input label={t('form.host')} placeholder={t('form.hostPlaceholder')} hint={t('form.hostHint')} />
<Input label={t('form.pathPrefix')} placeholder={t('form.pathPrefixPlaceholder')} hint={t('form.pathPrefixHint')} />
<Input label={t('form.backend')} placeholder={t('form.backendPlaceholder')} />
<Switch label={t('form.stripPrefix')} description={t('form.stripPrefixDescription')} />
<Button variant="ghost" onClick={onCancel}>{t('common:actions.cancel')}</Button>
<Button type="submit">{route ? t('form.update') : t('form.create')}</Button>
```

For the remaining files, use the file-specific snippets below instead of leaving any hardcoded labels behind:

```tsx
// AuthRulesPage.tsx
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('authRules')

const typeLabel = (value: string) => {
  switch (value) {
    case 'none':
      return t('types.none')
    case 'apikey':
      return t('types.apikey')
    case 'bearer':
      return t('types.bearer')
    case 'basic':
      return t('types.basic')
    case 'gateway':
      return t('types.gateway')
    default:
      return value
  }
}

if (loading) {
  return (
    <div className="flex h-64 items-center justify-center">
      <div className="flex items-center gap-3 text-[var(--text-muted)]">
        <div className="h-8 w-8 animate-spin rounded-full border-2 border-[var(--primary-500)] border-t-transparent" />
        {t('page.loading')}
      </div>
    </div>
  )
}

if (!confirm(t('page.deleteConfirm'))) return

const columns = [
  {
    key: 'route_id',
    header: t('table.protectedRoute'),
    render: (value: string) => <span className="font-semibold text-[var(--text-primary)]">{getRouteName(value)}</span>,
  },
  {
    key: 'type',
    header: t('table.authType'),
    className: 'w-40',
    render: (value: string) => <Badge variant="primary" badgeSize="sm">{typeLabel(value)}</Badge>,
  },
  {
    key: 'config',
    header: t('table.credentialMapping'),
    render: (value: any, row: AuthRule) => {
      if (row.type === 'apikey') return value.header_name ? `${t('form.headerName')} ${value.header_name}` : t('page.headerBasedKey')
      if (row.type === 'bearer') return t('page.bearerValidation')
      if (row.type === 'basic') return value.username ? `${t('form.username')} ${value.username}` : t('page.basicCredentials')
      if (row.type === 'gateway') return t('page.gatewayLogin')
      return t('page.noCredentials')
    },
  },
]

<PageHeader
  eyebrow={t('page.eyebrow')}
  title={t('page.title')}
  description={t('page.description')}
  meta={
    <>
      <Badge variant="primary">{t('page.badge')}</Badge>
      <span className="text-sm text-[var(--text-muted)]">
        {t('page.activeRuleDefinitions', { count: rules.length })}
      </span>
    </>
  }
  action={
    canManageAuth ? (
      <Button icon={<Plus className="h-4 w-4" />} onClick={() => { setEditingRule(null); setShowForm(true) }}>
        {t('page.addRule')}
      </Button>
    ) : null
  }
/>

<Alert variant="error" title={t('page.errorTitle')} className="mb-5">
  {error}
</Alert>

<EmptyState
  icon={<Shield className="h-8 w-8" />}
  title={t('page.emptyTitle')}
  description={t('page.emptyDescription')}
  action={canManageAuth ? <Button onClick={() => setShowForm(true)}>{t('page.createFirst')}</Button> : undefined}
/>

<Modal
  open={canManageAuth && showForm}
  onClose={() => { setShowForm(false); setEditingRule(null) }}
  title={editingRule ? t('page.editModalTitle') : t('page.addModalTitle')}
>
```

```tsx
// AuthRuleForm.tsx
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('authRules')

const ruleTypeOptions = [
  { value: 'none', label: t('types.none') },
  { value: 'apikey', label: t('types.apikey') },
  { value: 'bearer', label: t('types.bearer') },
  { value: 'basic', label: t('types.basic') },
  { value: 'gateway', label: t('types.gateway') },
]

<Select
  label={t('form.route')}
  value={form.route_id}
  onChange={(event) => setForm({ route_id: event.target.value, type: form.type, config: form.config })}
  options={routes.map((route) => ({
    value: route.id,
    label: route.name || route.path_prefix,
  }))}
  required
/>

<Select label={t('form.type')} value={type} onChange={(event) => setForm({ route_id: form.route_id, type: event.target.value as AuthRule['type'], config })} options={ruleTypeOptions} required />
<Input label={t('form.headerName')} placeholder="X-API-Key" required />
<Input label={t('form.secret')} placeholder="secret-value" required />
<Input label={t('form.jwtSecret')} placeholder="jwt-signing-secret" hint={t('form.jwtSecretHint')} required />
<Input label={t('form.username')} placeholder={t('form.usernamePlaceholder')} required />
<Input label={t('form.password')} placeholder={t('form.passwordPlaceholder')} required />
<Select
  label={t('form.loginMode')}
  value={config.login_mode || 'form'}
  onChange={(event) => updateConfig({ login_mode: event.target.value })}
  options={[{ value: 'form', label: t('types.gatewayForm') }]}
  hint={t('form.loginModeHint')}
  required
/>
<Button variant="ghost" onClick={onCancel}>{t('common:actions.cancel')}</Button>
<Button type="submit">{rule ? t('form.update') : t('form.create')}</Button>
```

```tsx
// UsersPage.tsx
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('users')

const roleLabel = (value: string) => {
  switch (value) {
    case 'member':
      return t('roles.member')
    case 'viewer':
      return t('roles.viewer')
    case 'editor':
      return t('roles.editor')
    case 'admin':
      return t('roles.admin')
    default:
      return value
  }
}

if (loading) {
  return (
    <div className="flex h-64 items-center justify-center">
      <div className="flex items-center gap-3 text-[var(--text-muted)]">
        <div className="h-8 w-8 animate-spin rounded-full border-2 border-[var(--primary-500)] border-t-transparent" />
        {t('page.loading')}
      </div>
    </div>
  )
}

if (!confirm(t('page.deleteConfirm', { username: user.username }))) return

const columns = [
  {
    key: 'username',
    header: t('table.user'),
    render: (value: string, row: User) => (
      <div>
        <div className="font-semibold text-[var(--text-primary)]">{value}</div>
        <div className="mt-1 text-xs text-[var(--text-muted)]">
          {t('page.assignedRoutes', { count: row.route_ids?.length || 0 })}
        </div>
      </div>
    ),
  },
  {
    key: 'role',
    header: t('table.role'),
    className: 'w-32',
    render: (value: string) => <Badge variant="primary" badgeSize="sm">{roleLabel(value)}</Badge>,
  },
  {
    key: 'enabled',
    header: t('table.status'),
    className: 'w-32',
    render: (value: boolean) => <Badge variant={value !== false ? 'success' : 'default'} badgeSize="sm">{value !== false ? t('page.enabled') : t('page.disabled')}</Badge>,
  },
  {
    key: 'route_ids',
    header: t('table.accessScope'),
    render: (value: string[], row: User) => {
      if (row.role === 'admin' || row.role === 'editor') return t('page.allRoutes')
      if (!value || value.length === 0) return t('page.noAssignedRoutes')
      return t('page.routeCount', { count: value.length })
    },
  },
]

<PageHeader
  eyebrow={t('page.eyebrow')}
  title={t('page.title')}
  description={t('page.description')}
  meta={
    <>
      <Badge variant="primary">{t('page.badge')}</Badge>
      <span className="text-sm text-[var(--text-muted)]">{t('page.managedUsers', { count: users.length })}</span>
    </>
  }
  action={
    canManageUsers ? (
      <Button icon={<Plus className="h-4 w-4" />} onClick={() => { setEditingUser(null); setShowForm(true) }}>
        {t('page.addUser')}
      </Button>
    ) : null
  }
/>

<Alert variant="error" title={t('page.errorTitle')} className="mb-5">
  {error}
</Alert>

<Modal
  open={canManageUsers && showForm}
  onClose={() => { setShowForm(false); setEditingUser(null) }}
  title={editingUser ? t('page.editModalTitle') : t('page.addModalTitle')}
>
```

```tsx
// UserForm.tsx
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('users')

const roleOptions = [
  { value: 'member', label: t('roles.member') },
  { value: 'viewer', label: t('roles.viewer') },
  { value: 'editor', label: t('roles.editor') },
  { value: 'admin', label: t('roles.admin') },
]

<Input label={t('form.username')} placeholder="alice" required />
<Select
  label={t('form.role')}
  value={form.role}
  onChange={(event) => setForm({ username: form.username, password: form.password, role: event.target.value, enabled: form.enabled, route_ids: form.route_ids })}
  options={roleOptions}
  hint={needsRouteAssignments ? t('form.memberHint') : t('form.operatorHint')}
  required
/>
<Input
  label={user ? t('form.newPassword') : t('form.password')}
  type="password"
  value={form.password || ''}
  onChange={(event) => setForm({ username: form.username, password: event.target.value, role: form.role, enabled: form.enabled, route_ids: form.route_ids })}
  placeholder={user ? t('form.newPasswordPlaceholder') : t('form.passwordPlaceholder')}
  required={!user}
  hint={user ? t('form.newPasswordHint') : undefined}
/>
<Switch label={t('form.enabled')} description={t('form.enabledDescription')} checked={form.enabled} onChange={(event) => setForm({ username: form.username, password: form.password, role: form.role, enabled: event.target.checked, route_ids: form.route_ids })} />

<div>
  <div className="text-[11px] font-semibold uppercase tracking-[0.14em] text-[var(--text-muted)]">
    {t('form.accessEyebrow')}
  </div>
  <p className="mt-2 text-sm text-[var(--text-muted)]">
    {t('form.accessDescription')}
  </p>
</div>

{routes.length === 0 ? (
  <div className="rounded-[18px] border border-[var(--border-default)] bg-[var(--bg-card-soft)] px-4 py-5 text-sm text-[var(--text-muted)]">
    {t('form.noRoutes')}
  </div>
) : (
  <div className="space-y-3">
    {routes.map((route) => (
      <label
        key={route.id}
        className="flex items-center justify-between gap-4 rounded-[18px] border border-[var(--border-default)] bg-[var(--bg-card-soft)] px-4 py-3 shadow-[var(--shadow-sm)]"
      >
        <div className="min-w-0">
          <div className="text-sm font-semibold text-[var(--text-primary)]">{route.name || route.path_prefix}</div>
          <div className="mt-1 text-xs text-[var(--text-muted)]">
            {route.host || t('form.allHosts')} · {route.path_prefix}
          </div>
        </div>
      </label>
    ))}
  </div>
)}

<Button variant="ghost" onClick={onCancel}>{t('common:actions.cancel')}</Button>
<Button type="submit">{user ? t('form.update') : t('form.create')}</Button>
```

```tsx
// SettingsPage.tsx
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('settings')

try {
  const result = await configApi.reload()
  setMessage(result.message)
} catch (e) {
  if (e instanceof ApiError) {
    setError(e.message)
  } else {
    setError(t('page.reloadFallback'))
  }
}

<PageHeader
  eyebrow={t('page.eyebrow')}
  title={t('page.title')}
  description={t('page.description')}
/>

<Alert variant="success" title={t('page.successTitle')} className="mb-5">
  {message}
</Alert>
<Alert variant="error" title={t('page.errorTitle')} className="mb-5">
  {error}
</Alert>

<MetricCard
  label={t('page.reloadAccess')}
  value={canReload ? t('page.reloadGranted') : t('page.reloadRestricted')}
  hint={t('page.reloadAccessHint')}
  icon={<RefreshCw className="h-5 w-5" />}
  tone="primary"
/>
<MetricCard label={t('page.storage')} value="SQLite" hint={t('page.storageHint')} icon={<Database className="h-5 w-5" />} tone="accent" />
<MetricCard label={t('page.version')} value="0.1.0" hint={t('page.versionHint')} icon={<Server className="h-5 w-5" />} />

<div className="flex-1">
  <h3 className="text-lg font-semibold tracking-[-0.02em] text-[var(--text-primary)]">{t('page.reloadCardTitle')}</h3>
  <p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">
    {t('page.reloadCardDescription')}
  </p>
  <Button
    className="mt-5"
    loading={reloading}
    disabled={!canReload}
    onClick={handleReload}
    icon={<RefreshCw className="h-4 w-4" />}
  >
    {reloading ? t('page.reloadingButton') : t('page.reloadButton')}
  </Button>
</div>

<h3 className="text-lg font-semibold tracking-[-0.02em] text-[var(--text-primary)]">{t('page.storageTitle')}</h3>
<p className="text-sm text-[var(--text-muted)]">{t('page.storageSubtitle')}</p>
<p className="text-sm leading-6 text-[var(--text-muted)]">
  {t('page.storageBody', { path: 'data/auth-gate.db' })}
</p>

<h3 className="text-lg font-semibold tracking-[-0.02em] text-[var(--text-primary)]">{t('page.securityTitle')}</h3>
<p className="text-sm text-[var(--text-muted)]">{t('page.securitySubtitle')}</p>
<p className="text-sm leading-6 text-[var(--text-muted)]">{t('page.securityBody')}</p>

<h3 className="text-lg font-semibold tracking-[-0.02em] text-[var(--text-primary)]">{t('page.aboutTitle')}</h3>
<p className="text-sm text-[var(--text-muted)]">{t('page.aboutSubtitle')}</p>
<div className="space-y-1 text-sm text-[var(--text-muted)]">
  <p><strong>{t('page.aboutProduct')}</strong></p>
  <p>{t('page.aboutVersion', { version: '0.1.0' })}</p>
  {sessionUser ? <p>{t('page.aboutRole', { role: sessionUser.role })}</p> : null}
</div>
```

- [ ] **Step 5: Run the focused regression tests**

Run:

```bash
npm --prefix packages/web test -- src/components/Layout.test.tsx src/components/DataTable.test.tsx
```

Expected:

```text
PASS src/components/Layout.test.tsx
PASS src/components/DataTable.test.tsx
```

- [ ] **Step 6: Commit**

```bash
git add packages/web/src/pages/RoutesPage.tsx packages/web/src/pages/AuthRulesPage.tsx packages/web/src/pages/UsersPage.tsx packages/web/src/pages/SettingsPage.tsx packages/web/src/components/RouteForm.tsx packages/web/src/components/AuthRuleForm.tsx packages/web/src/components/UserForm.tsx packages/web/src/lib/i18n/resources/routes.ts packages/web/src/lib/i18n/resources/authRules.ts packages/web/src/lib/i18n/resources/users.ts packages/web/src/lib/i18n/resources/settings.ts packages/web/src/lib/i18n/resources/index.ts packages/web/src/components/Layout.test.tsx
git commit -m "feat(web): translate admin pages and forms"
```

## Task 5: Translate Certificates UI and Add Locale-Aware Date Regression Coverage

**Files:**
- Create: `packages/web/src/lib/i18n/resources/certificates.ts`
- Create: `packages/web/src/pages/CertificatesPage.test.tsx`
- Modify: `packages/web/src/lib/i18n/resources/index.ts`
- Modify: `packages/web/src/pages/CertificatesPage.tsx`
- Modify: `packages/web/src/components/CertificateForm.tsx`
- Test: `packages/web/src/pages/CertificatesPage.test.tsx`

- [ ] **Step 1: Write the failing certificate page regression**

Create `packages/web/src/pages/CertificatesPage.test.tsx`:

```tsx
import { screen, waitFor } from '@testing-library/react'
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { CertificatesPage } from './CertificatesPage'
import { renderWithI18n } from '../test/render'

vi.mock('../lib/api/certificates', () => ({
  certificatesApi: {
    list: vi.fn(),
    create: vi.fn(),
    delete: vi.fn(),
    renew: vi.fn(),
  },
}))

vi.mock('../lib/session-store', () => ({
  getSessionUser: () => ({
    username: 'admin',
    role: 'admin',
    permissions: {
      can_manage_routes: true,
      can_manage_auth: true,
      can_manage_users: true,
      can_view_logs: true,
    },
  }),
}))

describe('CertificatesPage i18n', () => {
  beforeEach(async () => {
    vi.useFakeTimers()
    vi.setSystemTime(new Date('2026-06-01T00:00:00Z'))
    const { certificatesApi } = await import('../lib/api/certificates')
    vi.mocked(certificatesApi.list).mockResolvedValue([
      {
        id: 'cert-1',
        name: 'Wildcard',
        domain: '*.example.com',
        cert_path: '/tmp/cert.pem',
        key_path: '/tmp/key.pem',
        status: 'active',
        not_after: '2026-06-20T00:00:00Z',
        created_at: '2026-05-01T00:00:00Z',
        updated_at: '2026-05-01T00:00:00Z',
      },
    ])
  })

  it('renders translated status and locale-aware dates in zh-CN', async () => {
    await renderWithI18n(<CertificatesPage />, { locale: 'zh-CN' })

    await waitFor(() => {
      expect(screen.getByText('有效')).toBeInTheDocument()
    })

    expect(screen.getByText('证书')).toBeInTheDocument()
    expect(screen.getByText('2026年6月20日')).toBeInTheDocument()
    expect(screen.getByText('剩余 19 天')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run:

```bash
npm --prefix packages/web test -- src/pages/CertificatesPage.test.tsx
```

Expected:

```text
FAIL because CertificatesPage still hardcodes English labels, English dates, and English relative expiry text.
```

- [ ] **Step 3: Add certificate resources and translate the page and form**

Create `packages/web/src/lib/i18n/resources/certificates.ts`:

```ts
export const certificatesEn = {
  page: {
    eyebrow: 'TLS Automation',
    title: 'Certificates',
    description: "Provision and manage SSL certificates via Let's Encrypt ACME with automatic DNS-01 validation.",
    badge: 'ACME',
    count: '{{count}} certificates',
    provision: 'Provision Certificate',
    loading: 'Loading certificates...',
    errorTitle: 'Certificate operation failed',
    activeCertificates: 'Active Certificates',
    activeCertificatesHint: 'Valid certificates ready for use.',
    inProgress: 'In Progress',
    inProgressHint: 'Certificate provisioning or renewal in progress.',
    failed: 'Failed',
    failedHint: 'Certificates that encountered errors.',
    registryEyebrow: 'Certificate Registry',
    registryTitle: 'ACME provisioned certificates',
    registryDescription: 'Certificates are automatically renewed 30 days before expiration using DNS-01 challenge for wildcard support.',
    emptyTitle: 'No certificates provisioned',
    emptyDescription: 'Provision your first certificate to enable HTTPS for your routes. Wildcard certificates are supported via DNS-01 challenge.',
    provisionFirst: 'Provision First Certificate',
    modalTitle: 'Provision Certificate',
    renew: 'Renew',
    renewing: 'Renewing...',
    renewConfirm: 'Renew certificate for "{{domain}}"? This will take a few minutes.',
    deleteConfirm: 'Delete certificate for "{{domain}}"? This will also remove the certificate files.',
    expires: 'Expires',
    created: 'Created',
    dnsProvider: 'DNS Provider',
    certificate: 'Certificate',
    status: 'Status',
    daysLeft: '{{count}} days left',
    noDate: '-',
  },
  status: {
    active: 'Active',
    pending: 'Pending',
    renewing: 'Renewing',
    failed: 'Failed',
  },
  form: {
    detailsEyebrow: 'Certificate Details',
    detailsDescription: 'Provide a name and domain for the certificate. Wildcard certificates use "*.example.com" format.',
    name: 'Certificate Name',
    namePlaceholder: 'My Wildcard Cert',
    nameHint: 'A friendly name to identify this certificate',
    domain: 'Domain',
    domainPlaceholder: '*.example.com',
    domainHint: 'Use *.example.com for wildcard certificates',
    providerEyebrow: 'DNS Provider',
    providerDescription: 'Select how DNS validation will be performed. CloudFlare and Route53 support automatic DNS-01 challenge.',
    provider: 'DNS Provider',
    cloudflareToken: 'CloudFlare API Token',
    cloudflareTokenHint: 'Create an API token in CloudFlare dashboard with Zone:DNS:Edit permission',
    route53AccessKey: 'AWS Access Key ID',
    route53Secret: 'AWS Secret Access Key',
    route53Region: 'AWS Region (optional)',
    route53RegionHint: 'Leave blank to use default region',
    provisionButton: 'Provision Certificate',
    provisioningButton: 'Provisioning...',
    manualTitle: 'Manual Mode:',
    manualDescription: "You will need to manually create DNS TXT records when prompted. This is useful for testing or when you don't have API access to your DNS provider.",
    invalidDomain: 'Invalid domain format. Use something like "example.com" or "*.example.com".',
    cloudflareRequired: 'CloudFlare API token is required',
    route53Required: 'AWS Access Key ID and Secret Access Key are required for Route53',
  },
  providers: {
    cloudflare: 'CloudFlare',
    route53: 'AWS Route53',
    manual: 'Manual (DIY)',
  },
} as const

export const certificatesZhCN = {
  page: {
    eyebrow: 'TLS 自动化',
    title: '证书',
    description: '通过 Let\\'s Encrypt ACME 和自动 DNS-01 校验来申请和管理 SSL 证书。',
    badge: 'ACME',
    count: '{{count}} 张证书',
    provision: '申请证书',
    loading: '正在加载证书...',
    errorTitle: '证书操作失败',
    activeCertificates: '有效证书',
    activeCertificatesHint: '当前可直接使用的有效证书。',
    inProgress: '处理中',
    inProgressHint: '证书申请或续期正在进行中。',
    failed: '失败',
    failedHint: '处理过程中出错的证书。',
    registryEyebrow: '证书注册表',
    registryTitle: '通过 ACME 申请的证书',
    registryDescription: '证书会在到期前 30 天通过 DNS-01 挑战自动续期，支持通配符证书。',
    emptyTitle: '尚未申请证书',
    emptyDescription: '申请第一张证书，为你的路由启用 HTTPS。支持通过 DNS-01 挑战申请通配符证书。',
    provisionFirst: '申请第一张证书',
    modalTitle: '申请证书',
    renew: '续期',
    renewing: '续期中...',
    renewConfirm: '确认续期证书“{{domain}}”？这可能需要几分钟。',
    deleteConfirm: '确认删除证书“{{domain}}”？这也会删除对应的证书文件。',
    expires: '到期时间',
    created: '创建时间',
    dnsProvider: 'DNS 提供商',
    certificate: '证书',
    status: '状态',
    daysLeft: '剩余 {{count}} 天',
    noDate: '-',
  },
  status: {
    active: '有效',
    pending: '待处理',
    renewing: '续期中',
    failed: '失败',
  },
  form: {
    detailsEyebrow: '证书详情',
    detailsDescription: '填写证书名称和域名。通配符证书请使用 "*.example.com" 格式。',
    name: '证书名称',
    namePlaceholder: '我的通配符证书',
    nameHint: '用于识别这张证书的友好名称',
    domain: '域名',
    domainPlaceholder: '*.example.com',
    domainHint: '通配符证书请使用 *.example.com',
    providerEyebrow: 'DNS 提供商',
    providerDescription: '选择执行 DNS 校验的方式。CloudFlare 和 Route53 支持自动 DNS-01 挑战。',
    provider: 'DNS 提供商',
    cloudflareToken: 'CloudFlare API Token',
    cloudflareTokenHint: '请在 CloudFlare 控制台创建具备 Zone:DNS:Edit 权限的 API Token',
    route53AccessKey: 'AWS Access Key ID',
    route53Secret: 'AWS Secret Access Key',
    route53Region: 'AWS 区域（可选）',
    route53RegionHint: '留空则使用默认区域',
    provisionButton: '申请证书',
    provisioningButton: '申请中...',
    manualTitle: '手动模式：',
    manualDescription: '系统会提示你手动创建 DNS TXT 记录。这适合测试场景，或你没有 DNS 服务商 API 访问权限时使用。',
    invalidDomain: '域名格式无效。请使用类似 "example.com" 或 "*.example.com" 的格式。',
    cloudflareRequired: '必须提供 CloudFlare API Token',
    route53Required: '使用 Route53 时必须提供 AWS Access Key ID 和 Secret Access Key',
  },
  providers: {
    cloudflare: 'CloudFlare',
    route53: 'AWS Route53',
    manual: '手动（DIY）',
  },
} as const
```

Modify `packages/web/src/lib/i18n/resources/index.ts`:

```ts
import { certificatesEn, certificatesZhCN } from './certificates'

export const resources = {
  en: {
    common: commonEn,
    layout: layoutEn,
    login: loginEn,
    accessLogin: accessLoginEn,
    routes: routesEn,
    authRules: authRulesEn,
    users: usersEn,
    certificates: certificatesEn,
    settings: settingsEn,
  },
  'zh-CN': {
    common: commonZhCN,
    layout: layoutZhCN,
    login: loginZhCN,
    accessLogin: accessLoginZhCN,
    routes: routesZhCN,
    authRules: authRulesZhCN,
    users: usersZhCN,
    certificates: certificatesZhCN,
    settings: settingsZhCN,
  },
} as const
```

Modify `packages/web/src/pages/CertificatesPage.tsx`:

```tsx
import React from 'react'
import { FileKey, Key, Plus, RefreshCw, ShieldCheck } from 'lucide-react'
import { useTranslation } from 'react-i18next'
import { DataTable } from '../components/DataTable'
import { PageHeader } from '../components/PageHeader'
import { CertificateForm } from '../components/CertificateForm'
import { Alert, Badge, Button, Card, EmptyState, MetricCard, Modal } from '../components/ui'
import { certificatesApi } from '../lib/api/certificates'
import { getSessionUser } from '../lib/session-store'
import type { Certificate as CertificateType } from '../lib/api/types'

export function CertificatesPage() {
  const { t, i18n } = useTranslation('certificates')
  const [certificates, setCertificates] = React.useState<CertificateType[]>([])
  const [loading, setLoading] = React.useState(true)
  const [error, setError] = React.useState('')
  const [showForm, setShowForm] = React.useState(false)
  const [renewingId, setRenewingId] = React.useState<string | null>(null)
  const canManageCertificates = getSessionUser()?.permissions?.can_manage_routes !== false

  const fetchData = React.useCallback(async () => {
    try {
      setError('')
      const data = await certificatesApi.list()
      setCertificates(data)
    } catch (e) {
      setError((e as Error).message)
    } finally {
      setLoading(false)
    }
  }, [])

  React.useEffect(() => {
    fetchData()
  }, [fetchData])

  const handleCreate = async (data: { name: string; domain: string; dns_provider: string; provider_config: Record<string, string> }) => {
    try {
      await certificatesApi.create(data)
      setShowForm(false)
      await fetchData()
    } catch (e) {
      setError((e as Error).message)
    }
  }

  const handleDelete = async (cert: CertificateType) => {
    if (!confirm(t('page.deleteConfirm', { domain: cert.domain }))) return
    try {
      await certificatesApi.delete(cert.id)
      await fetchData()
    } catch (e) {
      setError((e as Error).message)
    }
  }

  const handleRenew = async (cert: CertificateType) => {
    if (!confirm(t('page.renewConfirm', { domain: cert.domain }))) return
    try {
      setRenewingId(cert.id)
      await certificatesApi.renew(cert.id)
      await fetchData()
    } catch (e) {
      setError((e as Error).message)
    } finally {
      setRenewingId(null)
    }
  }

  const activeCount = certificates.filter((certificate) => certificate.status === 'active').length
  const pendingCount = certificates.filter((certificate) => certificate.status === 'pending' || certificate.status === 'renewing').length
  const failedCount = certificates.filter((certificate) => certificate.status === 'failed').length

  const statusVariant = (status: string) => {
    switch (status) {
      case 'active':
        return 'success'
      case 'pending':
      case 'renewing':
        return 'warning'
      case 'failed':
        return 'error'
      default:
        return 'default'
    }
  }

  const formatDate = (dateStr?: string) => {
    if (!dateStr) return t('page.noDate')
    return new Intl.DateTimeFormat(i18n.resolvedLanguage === 'zh-CN' ? 'zh-CN' : 'en-US', {
      month: 'short',
      day: 'numeric',
      year: 'numeric',
    }).format(new Date(dateStr))
  }

  const statusLabel = (status: string) => t(`status.${status}` as const)

  const columns = [
    {
      key: 'name',
      header: t('page.certificate'),
      render: (value: string, row: CertificateType) => (
        <div>
          <div className="font-semibold text-[var(--text-primary)]">{value}</div>
          <div className="mt-1 font-mono text-xs text-[var(--text-muted)]">{row.domain}</div>
        </div>
      ),
    },
    {
      key: 'status',
      header: t('page.status'),
      className: 'w-28',
      render: (value: string) => <Badge variant={statusVariant(value) as any} badgeSize="sm">{statusLabel(value)}</Badge>,
    },
    {
      key: 'not_after',
      header: t('page.expires'),
      className: 'w-36',
      render: (value: string) => {
        if (!value) return t('page.noDate')
        const date = new Date(value)
        const now = new Date()
        const daysLeft = Math.ceil((date.getTime() - now.getTime()) / (1000 * 60 * 60 * 24))
        return (
          <div>
            <div className="text-sm">{formatDate(value)}</div>
            {daysLeft > 0 && daysLeft <= 30 && (
              <div className="text-xs text-[var(--accent-500)]">{t('page.daysLeft', { count: daysLeft })}</div>
            )}
          </div>
        )
      },
    },
  ]

  if (loading) {
    return (
      <div className="flex h-64 items-center justify-center">
        <div className="flex items-center gap-3 text-[var(--text-muted)]">
          <div className="h-8 w-8 animate-spin rounded-full border-2 border-[var(--primary-500)] border-t-transparent" />
          {t('page.loading')}
        </div>
      </div>
    )
  }

  <PageHeader
    eyebrow={t('page.eyebrow')}
    title={t('page.title')}
    description={t('page.description')}
    meta={
      <>
        <Badge variant="primary">{t('page.badge')}</Badge>
        <span className="text-sm text-[var(--text-muted)]">{t('page.count', { count: certificates.length })}</span>
      </>
    }
    action={
      canManageCertificates ? (
        <Button icon={<Plus className="h-4 w-4" />} onClick={() => setShowForm(true)}>
          {t('page.provision')}
        </Button>
      ) : null
    }
  />

  <Alert variant="error" title={t('page.errorTitle')} className="mb-5">
    {error}
  </Alert>

  <MetricCard label={t('page.activeCertificates')} value={activeCount} hint={t('page.activeCertificatesHint')} icon={<FileKey className="h-5 w-5" />} tone="primary" />
  <MetricCard label={t('page.inProgress')} value={pendingCount} hint={t('page.inProgressHint')} icon={<RefreshCw className="h-5 w-5" />} tone="warning" />
  <MetricCard label={t('page.failed')} value={failedCount} hint={t('page.failedHint')} icon={<Key className="h-5 w-5" />} tone="error" />

  <div className="text-[11px] font-semibold uppercase tracking-[0.14em] text-[var(--text-muted)]">
    {t('page.registryEyebrow')}
  </div>
  <h2 className="mt-2 text-2xl font-semibold tracking-[-0.03em] text-[var(--text-primary)]">
    {t('page.registryTitle')}
  </h2>
  <p className="mt-2 text-sm text-[var(--text-muted)]">
    {t('page.registryDescription')}
  </p>

  <EmptyState
    icon={<FileKey className="h-8 w-8" />}
    title={t('page.emptyTitle')}
    description={t('page.emptyDescription')}
    action={canManageCertificates ? <Button onClick={() => setShowForm(true)}>{t('page.provisionFirst')}</Button> : undefined}
  />

  <DataTable
    columns={columns}
    data={certificates}
    onDelete={canManageCertificates ? handleDelete : undefined}
    extraActions={(row: CertificateType) => (
      canManageCertificates && row.status === 'active' ? (
        <button
          onClick={() => handleRenew(row)}
          disabled={renewingId === row.id}
          className="flex items-center gap-2 rounded-lg px-3 py-1.5 text-sm text-[var(--primary-600)] transition-colors hover:bg-[var(--bg-hover)] disabled:opacity-50"
        >
          <RefreshCw className={`h-4 w-4 ${renewingId === row.id ? 'animate-spin' : ''}`} />
          {renewingId === row.id ? t('page.renewing') : t('page.renew')}
        </button>
      ) : null
    )}
  />

  <Modal open={canManageCertificates && showForm} onClose={() => setShowForm(false)} title={t('page.modalTitle')}>
    <CertificateForm onSubmit={handleCreate} onCancel={() => setShowForm(false)} />
  </Modal>
```

Modify `packages/web/src/components/CertificateForm.tsx`:

```tsx
import React from 'react'
import { useTranslation } from 'react-i18next'
import { Alert, Button, Card, Input, Select } from './ui'

export function CertificateForm({ onSubmit, onCancel }: CertificateFormProps) {
  const { t } = useTranslation('certificates')
  const [form, setForm] = React.useState({
    name: '',
    domain: '',
    dns_provider: 'cloudflare',
  })
  const [providerConfig, setProviderConfig] = React.useState<Record<string, string>>({})
  const [error, setError] = React.useState('')
  const [submitting, setSubmitting] = React.useState(false)

  const handleDnsProviderChange = (value: string) => {
    setForm({ name: form.name, domain: form.domain, dns_provider: value })
    if (value === 'route53') {
      setProviderConfig({
        access_key_id: '',
        secret_access_key: '',
        region: '',
      })
      return
    }

    setProviderConfig({})
  }

  const updateProviderConfig = (key: string, value: string) => {
    setProviderConfig((current) => Object.assign({}, current, { [key]: value }))
  }

  const dnsProviderOptions = [
    { value: 'cloudflare', label: t('providers.cloudflare') },
    { value: 'route53', label: t('providers.route53') },
    { value: 'manual', label: t('providers.manual') },
  ]

  const handleSubmit = async (event: React.FormEvent) => {
    event.preventDefault()
    setError('')
    setSubmitting(true)

    const domainRegex = /^(\*\.)?([a-zA-Z0-9-]+\.)+[a-zA-Z]{2,}$/

  if (!domainRegex.test(form.domain)) {
    setError(t('form.invalidDomain'))
    setSubmitting(false)
    return
  }

  if (form.dns_provider === 'cloudflare' && !providerConfig.api_token) {
    setError(t('form.cloudflareRequired'))
    setSubmitting(false)
    return
  }

    if (form.dns_provider === 'route53' && (!providerConfig.access_key_id || !providerConfig.secret_access_key)) {
      setError(t('form.route53Required'))
      setSubmitting(false)
      return
    }

    try {
      await onSubmit({
        name: form.name,
        domain: form.domain,
        dns_provider: form.dns_provider,
        provider_config: providerConfig,
      })
    } catch (e) {
      setError((e as Error).message)
      setSubmitting(false)
    }
  }

  <form onSubmit={handleSubmit} className="space-y-5">
    {error && <Alert variant="error">{error}</Alert>}

    <Card tone="soft" className="space-y-5">
      <div>
        <div className="text-[11px] font-semibold uppercase tracking-[0.14em] text-[var(--text-muted)]">
          {t('form.detailsEyebrow')}
        </div>
        <p className="mt-2 text-sm text-[var(--text-muted)]">
          {t('form.detailsDescription')}
        </p>
      </div>

      <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
        <Input label={t('form.name')} value={form.name} onChange={(event) => setForm({ name: event.target.value, domain: form.domain, dns_provider: form.dns_provider })} placeholder={t('form.namePlaceholder')} required hint={t('form.nameHint')} />
        <Input label={t('form.domain')} value={form.domain} onChange={(event) => setForm({ name: form.name, domain: event.target.value, dns_provider: form.dns_provider })} placeholder={t('form.domainPlaceholder')} required hint={t('form.domainHint')} />
      </div>
    </Card>

    <Card tone="soft" className="space-y-5">
      <div>
        <div className="text-[11px] font-semibold uppercase tracking-[0.14em] text-[var(--text-muted)]">
          {t('form.providerEyebrow')}
        </div>
        <p className="mt-2 text-sm text-[var(--text-muted)]">
          {t('form.providerDescription')}
        </p>
      </div>

      <Select label={t('form.provider')} value={form.dns_provider} onChange={(event) => handleDnsProviderChange(event.target.value)} options={dnsProviderOptions} required />

      {form.dns_provider === 'cloudflare' ? <Input label={t('form.cloudflareToken')} type="password" value={providerConfig.api_token || ''} onChange={(event) => updateProviderConfig('api_token', event.target.value)} placeholder="cf_xxxxxxxxxxxxxxxxxxxxxxxxxxxx" required hint={t('form.cloudflareTokenHint')} /> : null}

      {form.dns_provider === 'route53' ? (
        <div className="space-y-4">
          <Input label={t('form.route53AccessKey')} value={providerConfig.access_key_id || ''} onChange={(event) => updateProviderConfig('access_key_id', event.target.value)} placeholder="AKIAXXXXXXXXXXXXXXXX" required />
          <Input label={t('form.route53Secret')} type="password" value={providerConfig.secret_access_key || ''} onChange={(event) => updateProviderConfig('secret_access_key', event.target.value)} placeholder="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" required />
          <Input label={t('form.route53Region')} value={providerConfig.region || ''} onChange={(event) => updateProviderConfig('region', event.target.value)} placeholder="us-east-1" hint={t('form.route53RegionHint')} />
        </div>
      ) : null}

      {form.dns_provider === 'manual' ? (
        <div className="rounded-[18px] border border-[var(--warning-500)]/30 bg-[var(--warning-500)]/10 px-4 py-4">
          <p className="text-sm text-[var(--text-primary)]">
            <strong>{t('form.manualTitle')}</strong> {t('form.manualDescription')}
          </p>
        </div>
      ) : null}
    </Card>

    <div className="flex flex-col-reverse justify-end gap-2 border-t border-[var(--border-default)] pt-4 md:flex-row">
      <Button variant="ghost" onClick={onCancel} className="w-full md:w-auto">
        {t('common:actions.cancel')}
      </Button>
      <Button type="submit" className="w-full md:w-auto" disabled={submitting}>
        {submitting ? t('form.provisioningButton') : t('form.provisionButton')}
      </Button>
    </div>
  </form>
```

- [ ] **Step 4: Run the certificate regression test**

Run:

```bash
npm --prefix packages/web test -- src/pages/CertificatesPage.test.tsx
```

Expected:

```text
PASS src/pages/CertificatesPage.test.tsx
```

- [ ] **Step 5: Run the full web test suite and production build**

Run:

```bash
npm --prefix packages/web test
npm --prefix packages/web run build
```

Expected:

```text
PASS all web Vitest suites
vite build completes successfully and writes packages/web/dist
```

- [ ] **Step 6: Commit**

```bash
git add packages/web/src/pages/CertificatesPage.tsx packages/web/src/components/CertificateForm.tsx packages/web/src/pages/CertificatesPage.test.tsx packages/web/src/lib/i18n/resources/certificates.ts packages/web/src/lib/i18n/resources/index.ts
git commit -m "feat(web): localize certificates workflows"
```

## Task 6: Manual Smoke Test and Final Cleanup

**Files:**
- Modify: none unless issues are found during smoke test
- Test: entire `packages/web` frontend

- [ ] **Step 1: Run the manual smoke checklist**

Run:

```bash
npm --prefix packages/web run dev
```

Verify manually:

```text
1. The control-plane login page defaults to browser language on a clean localStorage state.
2. The login page language switcher flips labels without reloading the page.
3. The route access login page language switcher works the same way.
4. The authenticated sidebar labels update immediately when the locale changes.
5. At least one routes/auth/users modal shows translated labels, placeholders, and cancel/update buttons.
6. Delete and renew confirm prompts use the active locale.
7. Certificates dates and “days left” copy follow the active locale.
8. Backend-provided API error strings still appear unchanged.
```

- [ ] **Step 2: If smoke is clean, create the final feature commit or PR branch summary**

```bash
git status --short
git log --oneline -n 5
```

Expected:

```text
Only the planned i18n changes are present, and the recent commit stack matches the task sequence.
```
